# SKIPPED

## 건너뛴 작업: 없음

지시받은 11개 작업을 모두 마쳤습니다. 30분 이상 막혀 넘긴 작업은 없습니다.

---

## 다만, 확인하지 못한 것 (읽어 주세요)

### 브라우저 실제 동작 검증

Claude 브라우저 확장이 연결돼 있지 않아(`Browser extension is not connected`)
**실제 브라우저에서 눈으로 확인하지는 못했습니다.** 대신 이렇게 대신했습니다.

- 모든 작업마다 `npm run build` 통과 확인
- CSS 색 토큰 정합성 스크립트 검사 (라이트/다크 토큰 짝 맞음, 정의 없는 `var()` 0건,
  본문에 남은 하드코딩 색 0건)
- 라이트/다크 팔레트 명암비 계산 (카테고리 8색 모두 8:1 내외로 동일 수준)
- 미사용 import 0건 검사
- 실제 서버를 띄워 `/`, `/pricing`, `/favicon.svg`, `/manifest.webmanifest`, `/sw.js`
  응답과 SPA 폴백 확인
- 생성한 아이콘 PNG 를 직접 열어 글리프가 제대로 그려졌는지 확인

**따라서 다음 세 가지는 사람이 폰에서 한 번 봐 주셔야 합니다.**

1. **실제 폰 설치** — 안드로이드 "앱 설치" / iOS "홈 화면에 추가" 가 뜨는지,
   설치 후 주소창 없이 뜨는지, 오프라인에서 화면이 열리는지
2. **380px 실제 레이아웃** — 코드 기준으로 잡아낸 문제(상단 버튼 넘침, 터치 영역,
   URL 제목 삐져나옴 등)는 고쳤지만, 실기기에서만 보이는 것이 남아 있을 수 있습니다
3. **다크 모드 눈으로** — 명암비는 수치로 맞췄지만 "보기에 편한지" 는 다른 문제입니다

### 로그인이 필요한 화면

`.env` 는 있지만 계정 비밀번호가 없어 로그인 이후 화면(오늘 탭·아카이브·모달)을
띄워 보지 못했습니다. 로그인 화면까지만 정적으로 확인했습니다.

---

## 작업 중 발견해 손대지 않고 남긴 것

`supabase/setup.sql` 이 v1.0 시점 그대로여서 지금 앱이 쓰는 테이블 3개와 컬럼 6개가
빠져 있습니다. **수정 금지 파일이라 손대지 않았고**, 따라잡기 SQL 을 `TODO-SQL.md` 에
모아 뒀습니다. 실행도 하지 않았습니다.
