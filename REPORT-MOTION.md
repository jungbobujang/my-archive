# REPORT-MOTION — 모션·마이크로인터랙션 1차

`main` 에서 작업했습니다. **라이브러리 0개, 스키마 변경 0줄, SQL 0줄.**
CSS transition/keyframes 와 FLIP(직접 구현) 두 가지로만 했습니다.
서비스워커 캐시는 `v7 → v8` 로 올렸습니다.

새 파일: `src/motion.js` (`DUR_MS`·`EASE`·`staggerDelay`·`useFlip`·`playOnce`·`reducedMotion`)

---

## 1. 원칙 (여기서 벗어나는 모션은 넣지 않았습니다)

| 원칙 | 지킨 방법 |
| --- | --- |
| 150~250ms · ease-out | `--mo-dur: 180ms` · `--mo-ease: cubic-bezier(.2,.8,.3,1)` 두 변수로 통일 |
| 목적은 피드백 (장식 금지) | 넣은 곳은 여섯 자리뿐 — 등장·hover·저장·재배치·삭제·빠른 저장 |
| transform·opacity 만 | keyframes 안의 속성을 브라우저 점검이 직접 읽어 확인 |
| `will-change` 남용 금지 | **한 번도 안 씁니다.** 짧고 드문 모션이라 레이어를 미리 만들 이유가 없습니다 |
| reduced-motion 이면 전부 끔 | CSS 한 블록 + JS 세 곳(`staggerDelay`·`useFlip`·`playOnce`) |

---

## 2. 넣은 것

| # | 자리 | 무엇이 보이나 |
| --- | --- | --- |
| ① | 목록 로드·검색 갱신 | 아래서 12px 떠오르며 페이드인, **카드당 25ms 스태거(12장까지)** |
| ② | 카드 hover (마우스만) | 2px 들림 + 그림자 한 단계 |
| ③ | 저장 직후 | 그 카드가 0.95 → 1 로 들어앉고 테두리가 잠깐 초록 |
| ④ | 카테고리·검색 전환 | 남는 카드가 **FLIP** 으로 미끄러져 재배치 |
| ⑤ | 빠른 저장 성공 | 입력줄이 한 번 뛰고 `✓ 저장됨` 이 잠깐 떴다 사라짐 |
| ⑥ | 썸네일 순서 바꾸기 | `[◀ 맨 앞]` 등으로 옮길 때 **슬라이드**(FLIP) |
| ⑦ | 휴지통 복원·영구 삭제 | 그 줄이 축소·페이드로 빠지고 나머지가 FLIP 으로 메움 |
| ⑧ | 초기 로드 | 스켈레톤 카드 6장 (이미 있던 것을 그대로 씁니다 — 빈 화면 순간 없음) |

**③ 은 저장 id 를 넘겨야 됩니다.** `onSaved(warn)` → `onSaved(warn, savedId)` 로 넓혔습니다.
목록 쪽이 '어느 카드가 방금 들어온 것인지' 를 알아야 그 자리에 눈이 가게 만들 수 있습니다.

---

## 3. 만들면서 잡은 것 두 가지 (둘 다 실제 버그였습니다)

**ㄱ. 등장 애니메이션이 중간에 잘렸다.** '새 카드인가' 를 `Set` 에 '봤다' 로만 적어 두면,
목록이 한 번 더 그려지는 순간(개수·소속을 뒤이어 받아 오면 늘 그렇습니다) 방금 뜨던 카드가
'이미 본 것' 이 되어 클래스가 떨어져 나갑니다. 반쯤 투명한 카드가 남습니다.
판정을 **카드마다 한 번 정하고 기억하도록**(`Map`) 고쳤습니다.

**ㄴ. FLIP 의 출발점이 12px 어긋났다.** 위치를 `getBoundingClientRect` 로 재고 있었는데,
그 값은 **transform 이 얹힌 뒤의 자리**입니다. 카드가 아직 등장 애니메이션(12px 아래) 중이면
그 자리가 '원래 위치' 로 기록되고, 다음 이동이 12px 어긋난 데서 출발해 한 번 튑니다.
브라우저 점검이 `28 vs 16` 으로 잡았고, `offsetLeft/offsetTop`(레이아웃 값 — transform·스크롤에
흔들리지 않음)으로 바꿔 고쳤습니다.

---

## 4. 안 한 것 (판단)

**사라지는 카드의 페이드아웃(④의 앞 절반)은 필터 전환에서는 넣지 않았습니다.**
필터가 바뀌면 그 카드는 이미 React 트리에서 빠진 뒤라, 페이드시키려면 목록을 두 벌 들고
사라질 것들의 위치까지 흉내 내 붙잡아 둬야 합니다. 그 복잡도가 얻는 것보다 큽니다.
🔴 대신 **우리가 시점을 아는 자리**(휴지통 복원·영구 삭제)에서는 실제로 페이드아웃합니다(⑦) —
거기서는 '지웠다' 는 사실 자체가 피드백이라 값이 큽니다.

---

## 5. 검증

| 점검 | 결과 |
| --- | --- |
| `npm run check` | ALL PASS (105) |
| `npm run check:files` | ALL PASS (400) — 모션 배선 13건 추가 |
| `npm run check:mobile` | ALL PASS (93) — 모션 17건 추가 |
| `npm run check:share` | ALL PASS (99) |
| `npm run build` | 통과 |

브라우저(1280px, 카드 **49장**)에서 실제로 잰 값:

- 등장 180ms · 스태거 `0,25,50,…,275ms` 그다음부터 `0` (12장 규칙)
- keyframes 안의 속성이 **transform·opacity 뿐**
- hover 규칙 `translateY(-2px)` 이 `(hover: hover) and (pointer: fine)` 안에만 있음
- FLIP: 되돌린 자리가 옛 자리와 **1px 안**, `transform 180ms …` 만 트랜지션, 끝나면 인라인 값 걷힘
- **49장 재배치 중 34ms 넘는 프레임 0개** (최악 17ms — 60fps 유지)
- `prefers-reduced-motion: reduce` 로 다시 띄우면 **애니메이션 `none` · 트랜지션 `0s` ·
  FLIP 인라인 transform 도 안 걸림**

🔴 빠른 저장 펄스(⑤)의 jsdom 판정은 **넣지 못했습니다.** 재는 방법이 없어서가 아니라,
jsdom + React 제어 입력에서 값 주입이 state 로 들어가지 않기 때문입니다(네이티브 setter 를
거쳐도 그렇습니다 — 같은 트리의 버튼 클릭은 정상으로 돕니다). 억지로 흉내 내면 재는 것이
화면이 아니라 우리 흉내가 됩니다. 클래스·요소가 붙는 자리까지는 판정에 남겼습니다.
