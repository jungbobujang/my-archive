# 자리비움 잠금 — 작업 결과

브랜치 `idle-lock`. 커밋만 했고 push 는 하지 않았습니다.

## ⚠️ 사람이 눈으로 볼 것 (아직 남음)

**잠금 화면 스크린샷 확인이 필요합니다.** 자동 점검은 "덮었다 / 불투명하다 / 뒤가 안 굴러간다"
까지만 재고, **정말 아무것도 비치지 않는지는 사람 눈이 봐야** 합니다.

- `npm run check:mobile` 을 돌리면 아래 세 장이 생깁니다
  (기본 위치: `node_modules/.cache/mobile-shots/`)
  - `lock-375.png` — 375px 라이트
  - `lock-375-dark.png` — 375px 다크
  - `lock-1280.png` — 1280px
- 이 화면들은 **뒤에 "가려져야 할 내용" 이라는 글자를 잔뜩 깔아 두고** 찍은 것입니다.
  스크린샷에 그 글자가 **한 자라도 보이면 안 됩니다.**
- 만든 사람이 세 장 모두 확인했을 때는 새는 곳이 없었습니다. 그래도 실제 기기에서
  한 번 더 봐 주세요 — 특히 노치가 있는 폰의 위아래 가장자리.

그리고 실제 앱에서 한 번 해 보셔야 확실한 것:

1. 설정에서 PIN 을 걸고, 스위치를 켜고, 5분으로 두고 자리를 비워 보기
2. 잠긴 뒤 헤더·탭·목록이 화면에서 사라졌는지
3. PIN 을 넣어 풀었을 때 보던 자리(탭·검색어)로 돌아오는지
4. 쓰던 글이 있는 채로 잠갔다 풀면 그 글이 되살아나는지

---

## 무엇을 만들었나

### 1. 기기 단위 옵트인 (기본 꺼짐)

- 설정 → **자리비움 잠금**. PIN 을 걸기 전에는 아무것도 켜지지 않습니다.
- PIN 을 걸면 **"이 기기에서 자리비움 잠금 사용"** 스위치가 나옵니다. 기본은 꺼짐.
- 설정은 전부 `localStorage` 에 있습니다 (`ma:lock:*:{사용자id}`).
  그래서 **DB 스키마 변경이 없습니다 — `supabase/setup.sql` 은 이번에 건드리지 않았습니다.**

계정이 아니라 기기에 건 이유: 자리를 뜨는 사정은 기기마다 다릅니다. 교무실 컴퓨터에서
켜고 싶은 것이 집 노트북까지 따라가면 결국 꺼 버리게 됩니다.

### 2. 유예시간 5 / 15 / 30분 (기본 15분)

- 입력·터치·스크롤·마우스 이동·휠이 이만큼 없으면 잠깁니다.
- 타이머를 걸어 두지 않고 10초마다 "마지막 움직임이 언제였나" 를 봅니다.
  노트북 덮개를 닫았다 열었을 때 그동안이 유휴였다는 것을 바로 알아보게 하려는 것입니다.

### 3. PIN 4자리 · 한 번에 해제

- 네 자리를 다 넣으면 확인 버튼 없이 바로 확인합니다.
- 틀리면 칸을 비우고 남은 횟수를 알립니다.
- **10회 실패하면 로그아웃**합니다(재로그인 요구). 그때 실패 횟수는 0 으로 돌려서,
  다시 로그인한 사람이 "남은 0회" 를 물려받지 않게 했습니다.

### 4. 수동 잠금 🔒 — 모든 기기에서

- 헤더의 자물쇠 버튼, 또는 `Ctrl`(`⌘`)+`Shift`+`L`.
- **자동 잠금 스위치를 꺼 둔 기기에서도 작동합니다.** 평소에는 아무 비용이 없다가
  자리를 뜰 때 한 번 누르는 쪽이 대부분의 경우에 맞다고 보았습니다.
- 사람이 직접 잠근 것이므로 아래의 2시간 유예를 무시합니다(눌렀는데 안 잠기면 안 되니까).

### 5. 해제 후 2시간 유예

- 풀고 나면 2시간 동안은 유휴여도 자동으로 다시 잠기지 않습니다.
- 앉아서 생각만 하고 있어도 유휴로 세기 때문에, 유예가 없으면 풀자마자 다시 잠깁니다.

### 6. 잠긴 동안 우회 차단

- **본문을 가리는 것이 아니라 아예 그리지 않습니다.** `Archive` 가 잠금 상태면
  화면을 그리기 전에 빠져나가고 `LockScreen` 만 남습니다.
  반투명으로 덮거나 `display:none` 으로 숨기면 글자가 DOM 에 남아 개발자 도구·읽어 주기
  기능으로 그대로 읽힙니다. 그래서 덮지 않고 내렸습니다.
- 헤더·탭·목록·모달이 통째로 없으므로 탭 전환으로 우회할 자리가 없습니다.
- 내보내기·가져오기는 함수 안에서도 한 번 더 막습니다. 내보내기를 누른 뒤 파일을 만드는
  도중에 잠기면 **내려받지 않고 멈춥니다** — 잠긴 화면 뒤에서 파일이 떨어지면
  가려 둔 내용이 그대로 새어 나가기 때문입니다.
- 잠긴 동안 뒤 페이지를 손가락으로 굴릴 수 없게 막았습니다.

**상태는 살아 있습니다.** `Archive` 자신은 마운트된 채로 있어서 탭·검색어·열려 있던 모달이
그대로 남고, 풀면 있던 자리로 돌아옵니다. 쓰던 글은 기존 초안 기능이 받아 둡니다.

### 7. PIN 이 없으면 기능 자체가 없음

- PIN 미등록이면 자동 잠금도, 🔒 버튼도, 단축키도 나타나지 않습니다. 기존 사용 흐름 그대로입니다.
- **지시와 다르게 한 곳이 하나 있습니다.** "🔒 버튼은 모든 기기에 표시" 라고 하셨는데,
  **PIN 이 없는 기기에서는 숨겼습니다.** PIN 이 없으면 잠가도 풀 방법이 없어서
  로그아웃 말고는 나갈 길이 없는 화면이 되기 때문입니다.
  "자동 잠금 스위치가 꺼진 기기에서도 🔒 는 쓸 수 있다" 는 요구는 그대로 지켰습니다.
- PIN 을 지우면 스위치·실패 횟수·유예도 함께 지웁니다.

---

## PIN 을 어떻게 두었나 (그리고 한계)

평문으로 저장하지 않습니다. **PBKDF2 / SHA-256 / 20만 회 / 임의 소금 16바이트**로 바꿔
`localStorage` 에 둡니다. 소금은 저장할 때마다 새로 뽑으므로 같은 PIN 이라도 저장값이 다릅니다.

**한계를 분명히 적어 둡니다.** 네 자리는 경우의 수가 1만 개뿐입니다. 한 번 맞춰 보는 데
0.1초쯤 걸리게 만들어 두었으니 전부 훑으면 20분 남짓입니다.
**기기를 손에 넣고 파일을 뜯어보는 사람은 막지 못합니다.**
이 기능이 막는 것은 잠깐 자리를 비운 사이 지나가던 사람이 화면을 보는 일이고,
진짜 접근 통제는 Supabase 로그인이 합니다.

`crypto.subtle` 이 없는 환경(https 가 아닌 곳)에서는 기능을 열지 않고 그 이유를
설정 화면에 적습니다. 평문으로 물러서지 않습니다.

---

## 점검

세 가지 모두 통과했습니다. 괄호 안은 이번에 늘어난 수입니다.

- `npm run check` — **105 통과** (56 → 105, +49)
- `npm run check:files` — **118 통과** (그대로, 회귀 없음)
- `npm run check:mobile` — **45 통과** (31 → 45, +14)
- `npm run build` — 통과

새로 본 것들:

- PIN 규칙, 저장값에 평문이 없는 것, 소금이 매번 달라지는 것, PBKDF2 반복 횟수
- 맞는/틀린/자릿수 모자란 PIN
- 기본값이 꺼짐인 것, 기기 스위치, 5·15·30분 선택과 이상한 값의 되돌림
- 2시간 유예가 걸리고 풀리는 것, 수동 잠금이 유예를 걷어내는 것
- 실패 횟수 누적과 10회에서의 초기화
- 잠금 화면: 숫자만 받는 것, 값이 가려지는 것, 틀렸을 때의 안내와 남은 횟수,
  맞았을 때 열리고 실패 횟수가 0 이 되며 유예가 걸리는 것
- 설정 화면: PIN 없을 때/있을 때 보이는 것, 두 번 넣은 값이 다르면 막는 것
- 375px·1280px, 라이트·다크에서 가림막이 불투명하고 화면을 다 덮으며
  네 귀퉁이 어디를 집어도 가림막이 집히는 것, 굴러가도 계속 덮는 것
- `Archive` 가 잠기면 본문보다 **먼저** 빠져나가는지 (반투명 덮기로 되돌아가면 실패합니다)

한 가지 짚어 둡니다. 점검 중에 `overflow: hidden` 이 **사람이 굴리는 것만** 막고
`scrollTo` 같은 코드는 그대로 굴린다는 것을 확인했습니다. 그래서 "뒤가 안 굴러간다" 대신
"손가락으로 굴릴 수 없다 + 굴러가더라도 가림막은 계속 덮는다" 를 재도록 고쳤습니다.

---

## 건드린 파일

새로 만든 것

- `src/lock.js` — 기기 설정·PIN 해시·실패 횟수·유예 (React 없음)
- `src/components/LockScreen.jsx` — 잠금 화면

고친 것

- `src/hooks.js` — `useIdleLock` 추가
- `src/components/Archive.jsx` — 잠금 상태, 🔒 버튼, 단축키, 이른 return, 내보내기·가져오기 차단
- `src/components/Settings.jsx` — 잠금 설정 칸
- `src/styles.css` — 잠금 화면·설정 칸 스타일
- `scripts/check-modals.body.mjs` — 잠금 점검 49종
- `scripts/check-mobile.mjs` — 잠금 화면 실측 14종, `fullPage`·`waitSel` 옵션
- `scripts/mobile-harness/main.jsx` — `?mode=lock`
- `README.md`

**DB 변경 없음.** `supabase/setup.sql` 도, `TODO-SQL.md` 도 그대로입니다.
