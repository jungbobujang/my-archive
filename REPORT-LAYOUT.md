# REPORT-LAYOUT — 레이아웃 2차 (콘텐츠를 주인공으로)

브랜치 `layout-v2`. **커밋만 했고 푸시하지 않았습니다** (스크린샷 검수 뒤에 올립니다).
라이브러리 0개, 스키마 변경 0줄, SQL 0줄.

---

## 1. 결과 — 첫 화면에 보이는 카드 (before → after)

같은 자료(항목 49개, 이미지 33장·글 16장, 카테고리 8개)로 실제 `Archive` 를 띄워 쟀습니다.

| 화면 | before | after | 목록이 시작하는 높이 |
| --- | --- | --- | --- |
| **1280×900** | 완전 **4장** (일부 4장) | 완전 **11장** (일부 16장) | 663px → **340px** |
| 375×812 | 완전 **0장** (일부 1장) | 완전 **4장** (일부 6장) | 763px → **416px** |

요구선(1280px 에서 6장 이상)은 넘었습니다. 재는 법은 저장소에 남겼습니다:
`npm run measure:layout` — 첫 화면에 **완전히** 들어온 카드와 걸친 카드를 따로 셉니다.

### 스크린샷

| | 경로 |
| --- | --- |
| before 1280px | `docs/shots/layout-before-1280.png` |
| before 375px | `docs/shots/layout-before-375.png` |
| **after 1280px** | `docs/shots/layout-after-1280.png` |
| **after 375px** | `docs/shots/layout-after-375.png` |

---

## 2. 무엇을 바꿨나

**① 카테고리: 카드 8장 그리드 → 가로 한 줄 칩** (`289px` → `36px`)
`[전체 49] [💡 아이디어 7] [🎬 유튜브 대본 6] …` 가로 스크롤, 고른 칩은 검게 채웁니다.
카테고리 색은 칩 전체가 아니라 **왼쪽 점**으로만 냅니다 — 여덟 개가 나란히 물들면 시끄럽고,
정작 어느 것이 선택됐는지가 안 보입니다. 관리(추가·수정)는 줄 끝 `⚙` 로 들어갑니다.
🔴 줄바꿈하지 않습니다. 개수에 따라 줄 수가 바뀌면 목록이 시작하는 자리도 오르내려서
같은 화면이 매번 다르게 보입니다.

**② 검색 + 빠른 저장 한 줄** (입력줄 2개 96px → 1개 48px)
기본은 검색입니다. `[+]` 로 저장 모드로 가고, **친 것이 주소면 저절로** 넘어갑니다
(주소를 검색창에 붙여 넣는 일은 거의 언제나 '이거 저장해 줘' 입니다).
🔴 주소를 알아채도 **그 자리에서 저장하지는 않습니다.** 붙여 넣고 제목을 덧붙이는 일이
흔하고, '검색하려 했는데 저장돼 버리는' 쪽이 되돌리기 어렵습니다.
🔴 저장을 마치면 검색으로 되돌아옵니다. 저장 모드로 남겨 두면 다음에 무심코 친 낱말이
새 항목이 됩니다 — 조용히 쌓이는 쓰레기라 알아채기도 늦습니다.

**③ 갤러리 → 메이슨리 (CSS `columns`)**
라이브러리를 쓰지 않았습니다. `columns` 는 브라우저가 하는 일이라 카드가 몇 장이든
우리가 계산할 것이 없고 창을 줄이면 열 수가 알아서 바뀝니다.
그 대가로 **읽는 순서가 세로**가 됩니다(1열을 다 채우고 2열). 시간순 목록이라 위에서
아래로 최신순 한 덩어리씩 읽히므로 오히려 자연스럽습니다.

**④ 이미지가 곧 카드**
이미지 높이 고정(120px 크롭)을 없애고 원본 비율로 폅니다. 제목은 사진 아래쪽에
그라데이션 위 한 줄로 얹었습니다. 사진 위에서는 뱃지 줄을 감춥니다 — 색 상자를 겹치면
정작 사진이 안 보이고, 같은 정보는 카드를 열면 다 있습니다.
🔴 다만 **높이 상한**을 뒀습니다 (데스크톱 300px · 폰 200px). 400×900 같은 사진은
300px 폭에서 675px 이 되어 그 한 장으로 첫 화면이 끝납니다. 비율은 살리되 극단만 자릅니다.

**⑤ 갤러리일 때만 통을 넓힘** (880px → 1280px, 1000px 이상 화면)
880px 안에서는 넓은 화면에서도 세 열이 한계라 화면의 3분의 1이 여백이었습니다.
🔴 **글을 읽는 자리(리스트·오늘·모달)는 880px 그대로**입니다. 한 줄이 길어지면 읽기가
나빠집니다. 넓혀도 되는 것은 '훑는 화면' 뿐입니다.

**⑥ 유지**: 뷰 전환(갤러리/리스트/맵)·오늘 탭·태그 줄·필터 칩·휴지통 모두 그대로입니다.

---

## 3. 모션 1차와의 정합 (⑦)

메이슨리에서도 **등장 스태거와 FLIP 이 그대로 돕니다** (1280px·375px 양쪽 판정).
다만 FLIP 의 위치 재는 법을 바꿔야 했습니다:

🔴 `offsetLeft/offsetTop` 은 **다단(columns)에서 못 씁니다.** 요소가 단 조각 안에 놓여
화면에서 보이는 자리와 어긋납니다. 그래서 `getBoundingClientRect` 로 돌아가되,
지금 걸린 transform 의 이동분을 **빼서** 원래 자리를 복원하고 스크롤을 더해 문서 좌표로
만듭니다 (`restPosition`). 이렇게 하면 등장 애니메이션 중이어도, 다단이어도 정확합니다.
— 모션 1차에서 `offset*` 으로 바꿨던 이유(등장 중 12px 어긋남)도 이 방법으로 함께 해결됩니다.

---

## 4. 안 한 것 · 남는 것

- **폰(375px)의 필터 칩 줄**: `[할 것] [★ 중요]` 가 입력칸 아래로 감싸져 한 줄을 더 씁니다.
  입력줄은 하나이므로 요구(두 입력줄이 쌓이지 않기)는 지켰고, 이걸 더 줄이려면 필터를
  카테고리 칩 줄에 섞어야 하는데 그러면 '카테고리 고르기' 와 '상태 거르기' 가 한 줄에서
  섞입니다. 지금은 그대로 두고 여기 적어 둡니다.
- **가상 스크롤**: 49장에서는 필요 없습니다. 카드가 수백 장이 되면 그때 봅니다.

---

## 5. 검증

| 점검 | 결과 |
| --- | --- |
| `npm run check` | ALL PASS (105) |
| `npm run check:files` | ALL PASS (408) — 레이아웃 배선 10건 추가 |
| `npm run check:mobile` | ALL PASS (120) — 레이아웃·정합 27건 추가 |
| `npm run check:share` | ALL PASS (99) |
| `npm run build` | 통과 |

브라우저에서 재는 것: 첫 화면 카드 수 · 카테고리 카드 그리드가 0인가 · 칩이 한 줄인가 ·
입력칸이 하나인가 · 다단인가 · 카드가 단 경계에서 잘리지 않는가 · 이미지가 카드 폭을
채우는가 · 제목이 오버레이인가 · 가로 스크롤이 없는가 · **메이슨리에서 스태거와 FLIP 이
도는가**.

🔴 점검 하네스가 이번에 **가짜 Supabase 로** 바뀌었습니다. 레이아웃을 재려면 목록을 실제로
받아 와야 하는데, 진짜 클라이언트를 쓰면 화면 모양을 재는 일이 남의 서버와 그날의 연결
상태에 달리게 됩니다. 가짜에 `or`·`gt`·`count` 를 더해 그렇게 만들었습니다.
