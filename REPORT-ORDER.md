# REPORT-ORDER — 항목 모달의 이미지·파일 순서 바꾸기

`main` 에서 작업했습니다. **스키마 변경 0줄, SQL 0줄, DB 작업 0건.**
배열 순서가 곧 표시 순서라 열은 그대로 두고 순서만 바꿉니다
(`items.image_url` 의 줄 순서, `items.files` jsonb 배열 순서).
서비스워커 캐시는 `v3 → v4` 로 올렸습니다 (셸 번들이 바뀌었습니다).

---

## 1. 무엇이 생겼나

| 자리 | 조작 |
| --- | --- |
| 이미지 썸네일 줄 | 끌어서 순서 바꾸기 (마우스는 바로, 폰은 **꾹 누른 뒤**) · 썸네일마다 손잡이 `⠿` 에서 **←→** |
| 파일 목록 | 같은 방식의 끌기 + 줄마다 **▲▼** 버튼 |
| 안내 | 이미지가 2장 이상일 때 "썸네일을 끌면 순서가 바뀌고 맨 앞이 카드 표지예요 (폰은 꾹 누른 뒤 끌기)" |

한 장/한 개일 때는 손잡이도 ▲▼ 도 **아예 그리지 않습니다.** 바꿀 것이 없는데 조작만
보이면 눌러 보고 아무 일도 안 일어나는 자리가 됩니다.

새 파일: `src/reorder.js` (순수 함수 `moveItem` + 훅 `useDragOrder`).

---

## 2. 라이브러리를 쓰지 않은 근거

넣지 않았습니다. 세 가지 이유입니다.

1. **이 저장소에 화면 의존성이 없습니다.** react·supabase 둘뿐입니다. 정렬 하나 때문에
   dnd-kit(약 30KB) 을 들이면 번들이 커지고, 그 라이브러리의 접근성·터치 규칙을 이 화면의
   기존 규칙(손가락 영역 40px, 16px 글꼴 등)에 다시 맞춰야 합니다.
2. **HTML5 끌어놓기(`draggable`)는 여기서 못 씁니다.** 이미지·파일 영역이 이미
   `dragover`/`drop` 을 **파일 올리기**에 쓰고 있어서(`.img-drop` · `.file-drop`),
   같은 이벤트 위에 항목 끌기를 얹으면 둘이 서로를 가립니다. 게다가 iOS 사파리는
   HTML5 끌어놓기를 사실상 지원하지 않습니다 — 모바일 요구가 있는 이 일에는 애초에 답이 아닙니다.
3. **포인터 이벤트 하나로 마우스·터치·펜이 같은 코드로 처리됩니다.** 필요한 것은 그뿐입니다.

---

## 3. 모바일에서 스크롤과 부딪히지 않게 한 방법

- 터치는 **300ms 꾹 누른 뒤에만** 끌기가 시작됩니다. 그전에 10px 이상 움직이면
  "스크롤하려는 것" 으로 보고 끌기를 포기합니다.
- 마우스는 4px 움직여야 끌기로 봅니다 — 그래야 그냥 누르는 것(크게 보기)을 안 잡아먹습니다.
- 🔴 **`touch-action: none` 을 평소에 걸어 두지 않았습니다.** 그 값은 손가락이 닿는 순간에
  정해져서 길게 누른 뒤에 바꿔도 이미 시작된 제스처에는 안 먹고, 무엇보다 썸네일 줄에
  none 을 걸어 두면 **그 위에서는 화면을 아예 못 넘깁니다.** 순서를 바꿀 일이 없는
  사람에게는 그냥 고장입니다.
  대신 끌기가 **시작되는 순간에만** non-passive `touchmove` 리스너를 붙여 막고,
  손을 떼면 뗍니다. (React 의 `onTouchMove` 는 루트에 passive 로 붙어 `preventDefault`
  가 안 먹습니다. 그래서 네이티브 리스너를 직접 답니다.)
- 진동은 되면 주고 안 되면 그만입니다 (`navigator.vibrate?.(20)` — iOS 사파리는 없습니다).

---

## 4. 확인한 것

- **첫 이미지 = 카드 표지** 규칙은 그대로입니다(`ItemCard` 가 `parseImages(image_url)[0]`).
  순서를 바꾸면 표지도 따라 바뀝니다 — 점검에 넣었습니다.
- **순서만 바꿔도 '바뀜'(dirty)으로 잡힙니다.** 이미지는 `joinImages` 결과를 줄 순서까지
  비교하고, 파일은 경로를 순서대로 이어 비교하기 때문입니다. 실제로 순서만 바꾸고 Esc 를
  누르면 닫기 확인창이 뜹니다(점검 통과).
  초안(임시 보존)에도 이미지 순서가 그대로 들어갑니다. 파일은 원래 초안에 안 넣습니다.

---

## 5. 질문 필요 · 알아 두실 것

- 🔴 **이미 만들어 둔 공유 링크의 순서는 바뀌지 않습니다.** 공유 링크의 `files` 는
  링크를 만들 때 굳힌 스냅샷이라(REPORT-SHARE 참고), 항목에서 순서를 바꿔도 그 링크는
  만들 때의 순서를 그대로 보여 줍니다. 지시대로 **그대로 두었습니다** — 지난 스냅샷을
  나중에 바꾸면 '그때 보낸 것' 과 '지금 보이는 것' 이 달라져서, 공유 링크가 스냅샷이라는
  성질 자체가 무너집니다. 새 순서로 보내려면 링크를 다시 만들면 됩니다.
- 🔴 **모바일 스크롤 점검의 방법을 한 번 바꿨습니다.** 처음에는 실제로 굴려 보려고
  `Input.synthesizeScrollGesture(touch)` 를 썼는데, **끌기를 한 적이 없는 새 페이지에서도**
  안쪽 스크롤 상자를 굴리지 못했습니다(같은 좌표에서 마우스 제스처는 굴립니다).
  즉 그 실패는 우리 코드가 아니라 합성 터치의 한계였습니다. 그대로 두면 '멀쩡한데 빨간 줄'
  이 남아 다음 사람이 진짜 고장과 구분하지 못하므로, 스크롤을 **막는 두 수단**을 직접
  재는 쪽으로 바꿨습니다: ① 평소 `touch-action` 이 none 이 아닌지 ② 끌기가 끝나면
  `touchmove` 차단이 풀리는지(끌기 중에는 막히는지도 같이).

---

## 6. 검증

| 점검 | 결과 |
| --- | --- |
| `npm run check` (모달) | ALL PASS (105) |
| `npm run check:files` (파일·순서) | ALL PASS (345) — 순서 관련 27건 추가 |
| `npm run check:mobile` (375px) | ALL PASS (58) — 순서 관련 13건 추가 |
| `npm run check:share` (공유) | ALL PASS (93) |
| `npm run build` | 통과 |

새로 넣은 판정 중 중요한 것:

- `moveItem` 이 한 칸씩 정확히 옮기고 원본을 건드리지 않는다 (범위 밖·잘못된 자리 포함)
- 이미지 3장 순서를 뒤집고 → **저장 → 재열람** 했을 때 순서가 그대로다
- 그때 **카드 표지가 새 첫 장**으로 바뀐다
- 파일 3개를 ▲▼ 로 옮기고 → 저장 → 재열람 했을 때 순서가 그대로다
- 순서만 바꿔도 닫기 확인창이 뜬다
- 375px 에서 **꾹 눌러 끌면** 순서가 바뀐다 (실제 터치 이벤트로 확인)
- 375px 에서 평소 `touch-action` 이 묶여 있지 않고, 끌기가 끝나면 차단이 풀린다
- 손잡이 손가락 영역 38px, 파일 ▲▼ 34×28px
