# 무인 작업 보고 — 항목 파일 첨부 (file-attach)

- **브랜치**: `file-attach` (`main` = `2f0824f` 에서 새로 땀, main 은 손대지 않음)
- **푸시**: 하지 않음 — 로컬 커밋 14개 + 이 보고서 커밋 = 15개
- **검증**: `npm run check` 56 / `npm run check:files` **118(신규)** / `npm run check:mobile` 31
  → 기존 72종(56 + 16)은 그대로 통과, 새로 71종을 더했습니다.

> **2차 지시로 3건을 보완했습니다** (커밋 `5f76ea8`). 아래 표와 ⚠️ 절은 보완 뒤 기준으로 고쳐 두었습니다.
> ① 영구 삭제·비우기 때 **이미지도** 함께 삭제 ② 닫기 확인 문구에 **"첨부한 이미지/파일도 함께
> 삭제됩니다"** 명시 ③ 제목 자동 생성에서 **첫 파일 이름 반영 제거**. 자세한 것은 8절.
- **DB**: 실행하지 않았습니다. `supabase/setup.sql` 에 넣고 `TODO-SQL.md` 2절을 '실행 필요' 로 되돌렸습니다.
- 모든 작업마다 `npm run build` 통과를 확인한 뒤 커밋했습니다.
- `.env` 변경 0줄.

---

## 1. 작업별 결과

### ■ 저장

| # | 지시 | 결과 | 근거 · 남은 것 |
|---|---|---|---|
| 1 | files 버킷 (이미지와 분리) · SQL 은 실행 말고 기록 | ✅ | `archive-files` **비공개** 버킷 + 정책 3개를 `setup.sql` 6) 절에 추가. 실행 안 함. `TODO-SQL.md` 에 "실행 필요" 로 적음. 메타용 `items.files jsonb` 열도 같은 곳에. **버킷을 비공개로 한 것은 판단입니다 → 아래 ⚠️1** |
| 2 | 개당 10MB · 업로드 전 검사 · 초과 문구 | ✅ | `fileRejectReason()`. 문구는 정확히 `10MB 이하만 첨부할 수 있습니다 (현재 12.3MB)`. 10MB 정확히는 통과, +1바이트부터 거부(테스트 2종). 서버에도 `file_size_limit 10485760` 을 걸어 두 겹 |
| 3 | 항목당 최대 5개 | ✅ | 넣는 자리에서 한 번 더 막습니다 — 여러 개를 한꺼번에 떨어뜨리면 여유 계산이 겹칩니다 |
| 4 | 확장자 화이트리스트 8종, 그 외 거부 | ✅ | hwp·hwpx·pdf·docx·xlsx·pptx·txt·zip. exe·sh·bat·js·dmg·msi 거부 확인. 대문자(.PDF)·점 여러 개(`2026.1학기.기말.hwp`)도 통과. **확장자를 용량보다 먼저 봅니다** — 100MB 짜리 exe 에 "10MB 이하만" 은 엉뚱한 안내입니다 |
| 5 | 원본명 유지 · 키 `{항목id}/{시각}_{원본명}` · 한글 확인 | ⚠️ 완료(조건부) | 키 모양·한글 왕복 20종 통과. 다만 **새 항목은 아직 id 가 없어 임시 폴더** 를 씁니다 → ⚠️2. **실계정에서 한글 키가 받아들여지는지는 확인 못 했습니다** → 🔴실DB1 |

### ■ 화면

| # | 지시 | 결과 | 근거 · 남은 것 |
|---|---|---|---|
| 6 | 이미지 아래 '파일' 영역 · [파일 첨부] + 드롭 · 자동 라우팅 | ✅ | 이미지 영역 '아래' 인지는 DOM 순서가 아니라 **화면 좌표**(`file-drop.top ≥ img-drop.bottom`)로 확인. 라우팅은 이미지 칸·파일 칸 **양쪽** 에서 같게 동작합니다(`splitByKind`). 붙여넣기(Ctrl+V)는 손대지 않았습니다 → ⚠️6 |
| 7 | 아이콘+이름+용량 · 클릭 다운로드 · ✕ 삭제 | ✅ | 확장자별 아이콘 7종. 누르면 60초 서명 주소를 만들어 **원본 이름으로** 내려받습니다. ✕ 는 링크 목록의 `.link-x` 를 그대로 써서 좁은 화면 손가락 영역(40px)이 자동으로 붙습니다 |
| 8 | 카드에 📎N 뱃지 | ✅ | 개수만. 이름·용량은 싣지 않았습니다 — 카드 아래줄은 이미 카테고리·태그·링크가 다투는 자리입니다 |
| 9 | 사용량 게이지 `파일 ○○MB / 1GB` · 80% 주황 | ✅ | **설정 모달** 에 넣었습니다(사양에 자리 지정이 없어 판단 → ⚠️5). `items.files` 의 size 합계입니다. 80% 초과 시 주황 + "80% 를 넘었어요". 1GB 는 표시용 상수 → ⚠️5 |

### ■ 정합성

| # | 지시 | 결과 | 근거 · 남은 것 |
|---|---|---|---|
| 10 | 항목 삭제(휴지통 비우기 포함) 시 파일도 Storage 에서 삭제 | ✅ | 휴지통의 '영구 삭제'·'전부 비우기' 두 곳. 행을 먼저 지우고 파일을 지웁니다 — 뒤집으면 행 삭제가 실패했을 때 항목만 남고 파일이 사라집니다. 휴지통으로 보내는 것(soft delete)은 복원해야 하므로 지우지 않습니다. **2차 보완으로 이미지도 함께 지웁니다** — 유튜브 썸네일처럼 우리 버킷 밖의 주소는 거릅니다 |
| 11 | 고아 방지 · 이미지 쪽(ac74304)도 함께 봉합 | ✅ | 막은 자리 5곳: ①저장 없이 닫기(✕·취소·Esc) ②✕ 로 빼기 ③'새로 쓰기' 로 초안 버리기 ④휴지통으로 보내기 ⑤목록 화면 '빠른 저장' 의 insert 실패. **이미지도 같은 규칙** 입니다. 대신 ac74304 의 "붙여넣은 이미지를 초안에 남긴다" 는 뒤집혔고, **2차 보완으로 Esc 확인창이 그 사실을 알립니다** → ⚠️3 |
| 12 | 내보내기 JSON 은 메타만 + "실체는 별도" 명시 | ✅ | `items[].files` 가 행에 실려 그대로 나갑니다(`parseFiles` 로 한 번 걸러 path·name·size 세 가지만). 백업 JSON 최상위에 `files_note` 한 줄, 내려받은 뒤 토스트에도 "첨부 파일 실체는 별도". 가져오기는 그대로 — files 를 모르는 옛 백업도 같은 upsert 로 들어갑니다 |
| 13 | sessionStorage 임시 보존과 충돌 없는지 · 파일은 제외 | ✅ | **초안에 `files` 키가 없습니다**(테스트로 검사). 닫을 때 파일을 지우기 때문입니다 — 초안에 남기면 다시 열었을 때 이미 없는 파일을 가리킵니다. 글·링크·카테고리·이미지(이미 저장된 것)는 그대로 남습니다 |

### ■ 검증

| 지시 | 결과 | 근거 |
|---|---|---|
| 기존 72종 회귀 유지 | ✅ | `check` 56 + `check:mobile` 기존 16 = 72, 전부 PASS |
| 파일 첨부 검증 스크립트 추가 | ✅ | `scripts/check-files.mjs` (**118종**, `npm run check:files`). jsdom + esbuild 로 묶어 돌리는 방식은 `check-modals` 와 같고, `@supabase/supabase-js` 자리에 `scripts/fake-supabase.mjs` 를 alias 로 끼웁니다 |
| └ 10MB 초과 거부 | ✅ | 경계 3종(정확히 10MB / +1바이트 / 12.3MB 문구) |
| └ exe 거부 | ✅ | exe·sh·bat·js·dmg·msi 6종 + 확장자 없음 |
| └ 한글 파일명 왕복 | ✅ | 이름 4종 × 4가지(키 모양 · 평문 키 복원 · 인코딩 키가 ASCII 인지 · 인코딩 키 복원) + 이름 속 `/` 가 폴더가 되지 않는지 + 화면에 뜨는 이름·용량·아이콘 |
| └ 항목 삭제 후 고아 없음 | ✅ | 항목 3개 × (파일 2개 + 이미지 1장 + 유튜브 썸네일 1장)을 심고 한 건 영구 삭제 → **파일은 6→4, 이미지는 3→2 로 그 항목 것만 줄고 남은 항목 것은 그대로**, 전부 비우기 → 둘 다 0. 유튜브 썸네일은 지울 목록에 들지 않는 것까지 확인 |
| 실계정 필요한 것은 목으로 + 표기 | ✅ | 아래 🔴 실DB 확인 필요 5건 |
| 375px | ✅ | `check:mobile` 신규 15종. 가로 스크롤 0, 파일 영역이 이미지 영역 아래, 긴 이름이 ✕ 를 덮지 않음, ✕ 손가락 영역 40px, 첨부 버튼 44px, 기본 파일 위젯이 숨어 있음. 375·1280·다크 3종. 스크린샷 3장 추가 |

---

## 2. 🔴 실DB 확인 필요 (실계정 없이는 확정 못 함)

가짜 Supabase 로는 '우리 코드가 무엇을 보냈는가' 까지만 확인됩니다. 서버가 그것을 받아 주는지는 못 봤습니다.

1. **한글 저장 키를 실제 스토리지가 받는지.** 키에 원본 이름을 그대로 넣습니다
   (`item-1/1700000000000_2026학년도 계획서.hwp`). storage-api 판본에 따라 비ASCII 키를
   `Invalid key` 로 거부하는 것이 있습니다. 그래서 **그 오류일 때만** 퍼센트 인코딩한 키로
   한 번 물러서게 해 두었지만(`uploadFile`), **어느 쪽 길로 가는지는 실계정에서 봐야 합니다.**
   보이는 이름은 어느 쪽이든 원본입니다.
2. **비공개 버킷 + owner 정책이 실제로 동작하는지.** `auth.uid() = owner` 로 읽기·삭제를 막았습니다.
   `createSignedUrl` 은 읽기 권한이 있어야 만들어지므로, 정책이 어긋나면 **내려받기가 통째로 실패** 합니다.
   올린 뒤 바로 받아 보는 것으로 확인하세요.
3. **내려받을 때 파일 이름이 원본으로 저장되는지.** `createSignedUrl(..., { download: name })` 으로
   Content-Disposition 을 걸었습니다. 한글 이름이 브라우저에서 깨지지 않는지 봐야 합니다.
4. **서버 쪽 10MB 거부 문구.** 클라이언트 검사를 통과한 뒤에도 서버가 막으면
   "파일 N개를 올리지 못했어요" 로만 보입니다. 정확한 사유를 보여 주지 않습니다.
5. **`items.files` 열이 실제로 만들어졌을 때의 저장.** 지금 코드는 열이 없으면 그것만 빼고
   저장한 뒤 "supabase/setup.sql 을 실행해 주세요" 라고 알립니다(테스트로 확인). 열이 **있을 때**
   jsonb 왕복이 실제로 되는지는 SQL 을 실행한 뒤 한 번 붙였다 빼 보면 됩니다.

---

## 3. ⚠️ 판단이 들어간 곳 (되돌리기 전에 읽을 것)

지시에 답이 없어 제가 고른 자리입니다. 어느 것도 작업을 건너뛰지는 않았습니다.

### ⚠️1. 파일 버킷을 **비공개** 로 했습니다 (이미지 버킷은 공개)

지시는 "이미지 버킷과 분리" 까지였고 공개/비공개는 없었습니다. **기존 관행(공개)** 을 따르지
않은 유일한 자리입니다. 이미지와 달리 hwp·xlsx·zip 은 사람 이름과 성적이 들어가는 문서라,
주소만 알면 누구나 받을 수 있는 상태로 두는 쪽이 되돌리기 어려운 실수라고 봤습니다.
공개면 잘못 골랐을 때 유출이고, 비공개면 잘못 골랐을 때 요청이 한 번 더 붙을 뿐입니다.

> **판단 필요**: 공개 버킷으로 통일할지. 그렇게 하려면 `setup.sql` 6)절의 `public` 을 true 로,
> `signedFileUrl` 을 `getPublicUrl` 로 바꾸면 됩니다(둘 다 한 곳입니다).

### ⚠️2. 새 항목의 저장 키 앞자리가 `new-<uuid>` 입니다

지시의 키는 `{항목id}/...` 인데, **새 항목은 저장되기 전에는 id 가 없습니다.**
그런데 지시 11(저장 안 하고 닫으면 정리)은 파일이 저장 전에 이미 올라가 있다는 뜻이라,
둘을 동시에 만족시킬 수 없습니다. 수정 중인 항목은 지시대로 `{항목id}/` 를 쓰고,
새 항목만 이 모달에서만 쓰는 임시 id 를 씁니다. **지우기는 폴더가 아니라 경로 목록으로 하므로**
나중에 항목 id 와 달라도 기능에는 영향이 없습니다(테스트가 이것을 검사합니다).

> **판단 필요**: 저장 시점에 파일을 실제 항목 id 폴더로 옮길지(복사+삭제 한 번 더). 하지 않았습니다.

### ⚠️3. 저장하지 않고 닫으면 **붙여넣은 이미지도 사라집니다** (동작 변경)

ac74304 는 반대를 골랐습니다 — 올린 이미지를 초안에 남겨 두고, 그 대가로 스토리지에
고아를 남겼습니다(커밋 본문에 적혀 있습니다). 지시 11 이 그 구멍을 "이미지 쪽도 같은 방식으로
봉합" 하라고 해서 뒤집었습니다. 지우면서 초안에서도 함께 빼야 합니다 — 안 그러면 다시 열었을 때
이미 없는 그림을 가리킵니다.

**즉, 이전과 달라지는 것**: 스크린샷을 붙여넣고 저장하지 않은 채 닫으면 그 이미지는 없어집니다.
글·링크·카테고리·태그 초안은 그대로 남습니다.

**2차 보완**: <kbd>Esc</kbd> 로 닫을 때의 확인창이
`작성 중인 내용이 있습니다. 닫을까요? / 첨부한 이미지/파일도 함께 삭제됩니다.` 라고 알립니다.
이번에 올린 첨부가 있을 때만 붙습니다.

> 🔴 **남은 것**: **✕ 와 '취소' 는 원래 아무것도 묻지 않고 닫습니다**(모달 공통 규칙,
> 이번 작업 전부터 그랬습니다). 그래서 그 두 길로 닫으면 첨부가 **말없이** 지워집니다.
> 확인창을 새로 붙이는 것은 "그 외 변경 금지" 에 걸린다고 보아 하지 않았습니다.
> **판단 필요**: ✕·취소에도 같은 확인을 붙일지.

### ~~⚠️4. 영구 삭제 때 이미지는 지우지 않습니다~~ → **2차 보완으로 해결**

1차에서는 지시 10 이 "연결 **파일**" 이라고 해서 이미지를 두었습니다. 2차 지시로 통일했습니다.
이제 영구 삭제·전부 비우기에서 **파일과 이미지를 함께** 지웁니다.
유튜브 썸네일처럼 우리 버킷 밖의 주소는 `removeStorageImages` 가 걸러 냅니다
(경로를 못 뽑으면 지울 목록에 넣지 않습니다). 휴지통으로 보내는 것은 그대로 — 복원해야 합니다.

### ⚠️5. 게이지 자리와 1GB

- 사양에 자리 지정이 없어 **설정 모달** 에 넣었습니다. 항목 하나가 아니라 계정 전체의 값이라
  모달 안(항목 단위)에 두면 잘못 읽힙니다.
- **1GB 는 코드에 박은 표시값** 입니다(`STORAGE_QUOTA_BYTES`). 실제 플랜 용량을 읽어 오지
  않습니다 — 요금제 화면과 같은 사정입니다(구독 상태를 읽는 곳이 아직 없습니다).
- 세는 대상은 **항목에 붙어 있는 파일** 입니다. 스토리지를 직접 훑으면 폴더마다 요청이 붙고
  지워진 항목의 고아까지 세게 됩니다.
- `files` 열이 없는 DB 에서는 게이지를 **감춥니다**. 0MB 로 보이면 "아직 안 썼다" 로 읽힙니다.

### ~~⚠️6. 제목 자동 생성에 '첫 파일 이름' 을 끼웠습니다~~ → **2차 보완으로 되돌림**

사양 밖이라 뺐습니다. 순서는 다시 링크 제목 → 내용 앞 20자 → `이미지 YYYY-MM-DD` 입니다.
**파일만 붙이고 제목을 비운 항목은 `메모 YYYY-MM-DD` 로 저장됩니다.**

### 그 밖에 손대지 않은 것

- **붙여넣기(Ctrl+V)는 이미지만** 받습니다. 지시 6 은 "[파일 첨부] + 드래그앤드롭" 이라
  붙여넣기는 건드리지 않았습니다. 파일 탐색기에서 hwp 를 복사해 모달에 붙여넣으면 아무 일도
  일어나지 않습니다(예전과 같음).
- **이미지 영역은 아직 브라우저 기본 '파일 선택' 위젯** 입니다. 파일 영역만 버튼([📎 파일 첨부])이라
  나란히 놓으면 모양이 다릅니다. 이미지 쪽 UI 변경은 지시 범위 밖이라 두었습니다 → 🖼 스크린샷 확인 필요.
- `package-lock.json` 은 이번에도 손대지 않았습니다.

---

## 4. 🖼 스크린샷·실기기 확인 필요

수치로 확정할 수 없는 것만 적습니다. 스크린샷 3장을 새로 남겼습니다
(`node_modules/.cache/mobile-shots/modal-375-files.png`, `modal-375-files-dark.png`, `modal-1280-files.png`).

- **이미지 칸(기본 위젯)과 파일 칸(초록 버튼)이 위아래로 붙어 있는 모양.** 겹침·넘침은 수치로
  확인했지만 나란히 놓았을 때 보기 좋은지는 못 봤습니다(위 ⚠️ 마지막 항목).
- **모바일 브라우저의 내려받기.** 서명 주소를 `<a download>` 로 누르는 방식입니다.
  iOS 사파리는 `download` 속성을 무시하고 새 탭으로 여는 경우가 있어, 한글 이름 hwp 가
  '파일 앱' 으로 잘 떨어지는지는 폰에서 봐야 합니다.
- **게이지 색.** 80% 를 넘겼을 때의 주황(`--c-amber-mid`)이 다크 모드에서도 경고로 읽히는지.
- **긴 파일 이름의 말줄임 위치.** 375px 에서 `2026학년도 3학년 과학과 …` 로 잘립니다.
  잘린 뒤에도 무슨 파일인지 알아볼 수 있는지는 실제 파일 이름으로 봐야 합니다.

---

## 5. 다시 검증하는 방법

```
npm run build          # 빌드
npm run check          # 모달 동작 56종 (jsdom)
npm run check:files    # 파일 첨부 106종 (jsdom + 가짜 Supabase, 실계정 불필요)
npm run check:mobile   # 375px 실측 31종 + 스크린샷 (puppeteer)
```

도구가 없으면 `npm install --no-save jsdom puppeteer` 뒤에 다시 실행하세요.

`scripts/fake-supabase.mjs` 는 in-memory 버킷과 표를 들고 있는 가짜 클라이언트입니다.
esbuild 의 alias 로 `@supabase/supabase-js` 자리에만 끼우므로 앱 코드는 이것의 존재를 모릅니다.
**고아 파일 검사는 이 방법이 아니면 할 수가 없습니다** — 화면에는 아무것도 보이지 않기 때문입니다.

---

## 6. 커밋 (아래 14개 + 이 보고서, 로컬만 · 푸시 안 함)

```
5f76ea8  보완 3건: 영구 삭제 때 이미지도 · 닫기 문구에 첨부 명시 · 제목에서 파일 이름 뺌
e9c2c37  docs: REPORT-FILES.md — 파일 첨부 작업 결과표 (1차)
cff4636  fix(고아): '새로 쓰기' 로 초안을 버릴 때도 올려 둔 것을 지운다
c632ba2  docs: README 에 파일 첨부·고아 파일·점검 스크립트 반영
693d8cc  fix(고아): 빠른 저장이 실패하면 올려 둔 이미지를 되돌린다
a846be2  test: 375px 파일 영역 15종 + 사용량 게이지 4종
44cc883  test: 파일 첨부 점검 95종 (가짜 Supabase 로 고아까지 센다)
9c22391  feat(백업): 내보내기에 첨부 파일 메타만 담고 그 사실을 파일에 적는다
d9e3bc8  fix(정합성): 영구 삭제 때 첨부 파일도 스토리지에서 지운다
d09ebac  feat(설정): 첨부 파일 저장소 사용량 게이지 (80% 넘으면 주황)
7809aaf  feat(카드): 첨부 파일이 있으면 📎N 뱃지
2c2b657  feat(모달): 이미지 아래에 '파일' 영역 — 목록·내려받기·빼기·끌어놓기
f9663b1  feat(db): items.files 열 + 비공개 버킷 archive-files (실행은 사람이)
7fa28e5  feat(파일): 일반 파일 첨부 유틸 — 검증·저장키·업로드·서명 주소
```

```
 README.md                       |  62 ++++-
 TODO-SQL.md                     |  28 +++
 package.json                    |   1 +
 scripts/check-files.body.mjs    | 402 ++++++++++++++++++++++++++++++++++
 scripts/check-files.mjs         |  51 +++++
 scripts/check-mobile.mjs        |  56 ++++-
 scripts/fake-supabase.mjs       | 144 ++++++++++++
 scripts/mobile-harness/main.jsx |  12 +-
 src/components/Archive.jsx      |  29 ++-
 src/components/ItemCard.jsx     |   5 +-
 src/components/ItemModal.jsx    | 359 +++++++++++++++++++++++++-----
 src/components/Settings.jsx     |  57 ++++-
 src/components/Trash.jsx        |  12 +-
 src/styles.css                  |  76 +++++++
 src/supabase.js                 | 183 ++++++++++++++-
 supabase/setup.sql              |  46 +++-
```

## 7. 하지 않은 것

- **푸시** — 지시대로 로컬 커밋만.
- **SQL 실행** — `setup.sql` 에 넣기만 했습니다. `TODO-SQL.md` 2절을 보고 SQL Editor 에서 실행하세요.
- **사양에 없는 기능** — 없습니다(1차의 ⚠️6 도 2차에서 되돌렸습니다).
- **이미지 쪽 UI 통일** — 지시 범위 밖입니다(⚠️ 그 밖에 손대지 않은 것).
- **✕·취소에 확인창 붙이기** — 2차 지시가 "그 외 변경 금지" 라 하지 않았습니다(⚠️3 의 🔴).

---

## 8. 2차 보완 3건 (커밋 `5f76ea8`)

| # | 지시 | 결과 | 무엇을 했나 |
|---|---|---|---|
| 1 | 영구 삭제·비우기 때 이미지도 Storage 에서 삭제 | ✅ | `Trash.jsx` 의 `purge` · `purgeAll` 두 자리에 `removeStorageImages(parseImages(...))` 를 더했습니다. 행을 먼저 지우고 파일 → 이미지 순서입니다. 유튜브 썸네일 같은 바깥 주소는 걸러집니다 |
| 2 | 닫기 확인 문구에 첨부도 삭제된다고 명시 | ✅ | Esc 확인창이 `작성 중인 내용이 있습니다. 닫을까요? / 첨부한 이미지/파일도 함께 삭제됩니다.` 로 뜹니다. **이번에 올린 첨부가 있을 때만** 두 번째 줄이 붙습니다 — 이미 항목에 붙어 있던 것은 닫아도 그대로라 그렇게 말하면 거짓입니다. 🔴 ✕·취소는 원래 묻지 않고 닫습니다(⚠️3) |
| 3 | 제목 자동 생성에서 첫 파일 이름 제거 | ✅ | 한 줄을 뺐고 주석도 되돌렸습니다. 파일만 붙인 항목은 `메모 YYYY-MM-DD` 가 됩니다 |

점검 12종을 더했습니다(106 → 118): 삭제 뒤 이미지 고아 0 · 남은 항목 이미지 보존 ·
바깥 주소는 지우려 들지 않는 것 · 문구가 붙는 경우와 안 붙는 경우 · 취소하면 첨부가 남는 것 ·
제목에 파일 이름을 쓰지 않는 것.

`build` · `check`(56) · `check:files`(118) · `check:mobile`(31) 전부 통과 확인 뒤 커밋했습니다.
그 밖의 변경은 없습니다.
