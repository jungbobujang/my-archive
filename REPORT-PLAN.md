# REPORT-PLAN — 계획 격자 (지평 × 영역)

브랜치 `plan`. `main` 머지는 **검수 후**. SQL 은 **출력만** 했고 실행하지 않았습니다
(실행할 것은 `TODO-SQL.md` 의 「🔴 계획 격자 (plan)」 절에 그대로 있습니다).

---

## 0. 먼저: 이 작업을 시작할 때의 가정

지시에 **"열린 저장소가 my-archive가 아니면 멈추고 알릴 것"** 이 있었습니다.
이 세션이 열려 있던 저장소는 `C:\Users\user`(홈 디렉터리, 브랜치 `master`, 원격 없음)이라
가드에 걸려 **한 번 멈추고 보고했습니다.** 같은 지시가 다시 내려와서, 대상 저장소를
`C:\Users\user\Downloads\projects\my-archive` 로 **고정하고** 진행했습니다.

- 원격은 관행대로 SSH (`git@github.com:jungbobujang/my-archive.git`)
- 출근 동기화(`git pull --ff-only origin main`) → `Already up to date` (3afd321)
- 거기서 `plan` 브랜치를 땄습니다

---

## 1. 무엇을 만들었나 (요구사항 1–10)

| # | 요구 | 결과 |
|---|---|---|
| 1 | items 필드 4개 | `horizon` · `plan_status` · `domain` · `related_ids` **+ `plan_status_at`** (2절) |
| 2 | plan_domains 표 · 기본 6개 · 최대 8 | 표 + 시드(수입·성장·건강·창작·관계·생활) + 설정에서 이름·색·순서 |
| 3 | 세 번째 탭 "계획" 격자 | 가로 지평 3 × 세로 영역, 칸에 planned·doing만, done은 아래 접힌 목록, 셀 헤더 개수, 빈 칸 "비어 있음" |
| 4 | 카드 | 제목 + 왼쪽 색 띠(doing이면 굵게) + 누르면 상태 메뉴 + 길게 누르면 항목 모달 |
| 5 | 격자에 올리기 | 항목 모달 "계획으로" 토글(기본 꺼짐) + 아카이브 카드의 `🧭 계획으로` 버튼 |
| 6 | related_ids | 계획 칸에서 아카이브 항목 검색·연결, 카드에 🔗N |
| 7 | 오늘 탭 한 줄 | `🧭 진행 중 N · 이번 주 완료 N` → 누르면 계획 탭 |
| 8 | 주간 리뷰 | 우상단 📋 → 이번 주 완료 / 7일 정체 / 빈 칸, 셋 다 **목록으로** |
| 9 | 공간별 분리 · 375px 아코디언 | 격자 조회에 `space` 조건, ≤639px 에서 영역별 접이식 섹션(지평 3칸은 가로 유지) |
| 10 | 검증 | 기존 5종 회귀 + 신규 `check:plan` 176건 + 375px 실측 (5절) |

### 새 파일

```
src/plan.js                       지평·상태·영역 상수, 격자 만들기, 정체/이번 주 판정, 영역 CRUD
src/components/Plan.jsx           계획 탭 — 조회·상태 전환·완료 접기·주간 리뷰
src/components/PlanGrid.jsx       격자 그리기만 (순수). 375px 점검이 이것을 단독으로 띄운다
src/components/PlanCard.jsx       카드 한 장 — 색 띠·상태 메뉴·길게 누르기
src/components/PlanQuickAdd.jsx   아카이브 카드에서 바로 올리는 작은 창
scripts/check-plan.mjs(.body)     신규 점검 176건
```

---

## 2. 요구와 다르게 한 것 — `plan_status_at` 한 열을 더했습니다

요구사항 1은 열 **넷**이었고 다섯을 만들었습니다. 이유는 요구사항 8입니다:

> doing 중 **7일 이상 상태 변화 없는** 정체 목록

'상태가 언제 바뀌었나' 를 적어 두는 곳이 없으면 이 목록을 만들 수 없습니다. 후보는 셋이었습니다.

- **`updated_at` 을 쓴다** — 제목 오타 하나만 고쳐도 갱신됩니다. 그러면 7일째 멈춰 있던
  계획이 '방금 움직인 것' 이 되어 정체 목록에서 빠져나갑니다. **화면에는 아무 표시도 안 납니다** —
  리뷰가 조용히 비어 가고, 그 사실을 알아챌 방법이 없습니다.
- **`created_at` 을 쓴다** — 만든 지 오래된 계획이 전부 정체가 됩니다.
- **열을 하나 더 둔다** ← 골랐습니다. null 로 들어가고, 옛 행은 `created_at` 으로 읽습니다
  (`plan.js` `planStatusAt`) — 0 으로도, '지금' 으로도 읽지 않습니다.

상태가 **실제로 바뀔 때만** 찍습니다(`planFields({ statusChanged })`). 제목만 고친 저장은
이 열을 건드리지 않습니다.

---

## 3. 판단이 갈릴 수 있었던 자리

### 3-1. 영역은 공간별이 아니라 **계정 단위**

요구사항 9가 "공간(space)과 정합 / 계획도 공간별 분리" 라, 영역 축까지 공간마다 나눌 수도
있었습니다. 계정 단위로 두었습니다.

- 격자의 세로축은 '수입·성장·건강' 같은 **삶의 갈래**입니다. 서랍(개인/수업)을 바꾼다고
  건강이 건강이 아니게 되지는 않습니다.
- 공간마다 축을 두면 영역 6 × 공간 5 = **30줄**을 사람이 관리하게 되고,
  "최대 8개" 라는 약속이 무엇의 8개인지 흐려집니다.
- **계획이 공간별로 갈리는 일은 `items.space` 가 이미 합니다.** 격자에는 지금 서랍의
  계획만 담기므로, 축은 같고 내용이 갈립니다 — 요구사항 9는 그것으로 충족됩니다.

기존 관행과도 맞습니다: `time_slots`(계정 하나)와 `categories`(공간별) 중
이것은 전자에 가깝습니다.

### 3-2. `items.status` 와 합치지 않았습니다

`status`(none/todo/done)와 `plan_status`(planned/doing/done/dropped)는 **다른 열**입니다.
합치면 오늘 탭의 체크 하나가 계획의 진행 상태를 바꾸게 됩니다 — 서로 다른 두 질문에
한 번만 답하는 셈입니다. 대가는 열 하나이고, 헷갈릴 자리는 모달의 두 칸이 서로 다른
이름('실행 상태' / '계획')을 달고 있는 것으로 갈랐습니다.

### 3-3. 표를 나누지 않고 `items` 에 열을 더했습니다

계획은 항목의 한 **상태**이지 다른 종류의 자료가 아닙니다. 적어 둔 아이디어가 어느 날
계획이 되고, 끝나면 다시 기록으로 남습니다. 표를 나누면 그때마다 행을 옮겨야 하고,
첨부·링크·소속·공유 링크가 전부 `items.id` 를 가리키고 있어 같이 따라가야 합니다.
열 다섯을 더하면 옮길 것이 없고, 목록 조회(`select('*')`)·백업·휴지통·공유가 그대로입니다.
`space`·`files` 에서 이미 두 번 같은 판단을 한 자리입니다.

### 3-4. 영역 **지우기는 두지 않았습니다** (공간과 같은 규칙)

다만 공간과 달리, 지워진 영역을 가리키는 계획은 조용히 사라지지 않습니다 —
격자가 **'미지정' 줄**로 모아 보여 줍니다(`plan.js` `domainOf`). 그래도 버튼은 안 두었습니다:
여덟 줄짜리 축에서 지우기가 필요한 일은 드물고, 안 쓰는 줄은 이름만 바꿔 두면 됩니다.

### 3-5. '계획 아님' 은 열을 **비워서 보냅니다**

격자에서 내릴 때 payload 에서 계획 열을 빼면 `update` 가 그 열을 건드리지 않아,
내린 줄 알았던 계획이 격자에 그대로 남습니다. `planFields({isPlanned:false})` 가
`null` 을 명시해서 보냅니다. 점검 항목: *내리면 상태가 비워진다 / 지평도 / 영역도*.

### 3-6. 좁은 화면에서 지평은 **가로로 남깁니다**

요구사항 9 그대로입니다. 지평까지 세로로 쌓으면 8영역 × 3지평 = **24줄**이 되어
격자가 목록이 되고, '어디가 비었나' 를 한눈에 보는 일이 다시 스크롤이 됩니다.
375px 에서 칸 폭은 **110px** 로 측정됐습니다(실측).

접힘 상태는 CSS 가 아니라 JS(`useNarrow`)가 듭니다. CSS 로만 접으면 폰에서 접어 둔 영역이
넓은 화면에서도 사라져 보입니다.

---

## 4. 작업 중에 잡힌 실제 버그 하나

**375px 에서 긴 제목이 칸 밖으로 샜습니다.** 칸은 110px 인데 제목이 118px 로 그려지고
말줄임이 안 걸렸습니다.

원인은 `<button>` 을 flex 컨테이너로 쓴 것이었습니다 — 크롬에서 button 의 안쪽 내용은
최대 너비로 굳어서, 자식에 `flex:1 1 auto; min-width:0` 을 줘도 줄어들지 않습니다.
`.plan-card` 를 `display:block` 으로 두고 안에 `.plan-card-row`(flex) 를 한 겹 넣어 고쳤습니다.

`npm run check:mobile` 이 잡았습니다. 375px 실측을 안 돌렸으면 폰에서만 보이는 모양으로
머지됐을 자리입니다.

---

## 5. 검증

### 5-1. 회귀 — 기존 5종 전부 통과

```
npm run check          ALL PASS (105)   모달 동작
npm run check:files    ALL PASS (405)   파일 첨부 · 모션
npm run check:space    ALL PASS  (56)   공간 분리
npm run check:share    ALL PASS  (99)   공유 링크
npm run check:mobile   ALL PASS (136)   375px 실측 (계획 29건 포함)
npm run build          ✓ built           104 modules
```

### 5-2. 신규 — `npm run check:plan` **ALL PASS (176)**

15개 묶음이 무엇을 보는지:

1. **순수 함수** — 지평/상태/영역 기본값, 전환 메뉴, `planFields`, 주 시작(월요일),
   정체·이번 주 판정, `plan_status_at` 이 없을 때의 폴백
2. **격자 만들기** — 칸 배치, 완료/접음 분리, 계획 아닌 항목 제외, 미지정 줄,
   **빈 칸을 만들어 돌려주는지**, 빈 칸 세기에서 미지정 줄 제외
3. **격자 화면** — 3×3 칸, 빈 칸 "비어 있음" 7개, 셀·줄 헤더 개수, doing 굵은 띠,
   영역 색, 완료 접기/펴기
4. **상태 전환** — 메뉴 내용, `plan_status` **와 `plan_status_at` 이 함께** 쓰이는지,
   정체 해제, `✎ 자세히` 경로
5. **저장 실패** — 화면을 되돌리고 말해 주는지
6. **주간 리뷰** — 세 목록이 지표와 맞는지(예전 완료 제외, 막 시작한 것 제외, 며칠째)
7. **좁은 화면** — 아코디언 전환, 가로축 머리 접힘, **지평 3칸 가로 유지**, 접기/펴기
8. **공간 분리** — 격자 조회에 `space` 조건이 **실려 나가는지**(화면에서 거른 것이 아니라),
   계획만 물어보는지, 서랍 전환
9. **오늘 탭** — 숫자, 서랍별, 계획 0건이면 줄 없음, 눌러서 탭 이동
10. **항목 모달** — 기본 꺼짐, 켜면 네 칸, 저장 payload, **내리면 열이 비워지는지**
11. **관련 항목** — 검색이 자기 자신·이미 걸린 것·다른 서랍을 빼는지, 카드의 🔗N
12. **카드에서 올리기** — 지평·영역·상태·시각, 이미 올라간 카드는 자리를 적는지
13. **설정** — 이름·색·순서(position 맞바꿈), 최대 8개, 지우기 없음
14. **SQL 미실행 DB** — 탭 숨김, 한 줄 안내, 기억해 둔 계획 탭 대신 오늘,
    목록·저장 정상, **계획 열을 보내지 않는지**
15. **백업** — `plan_domains` 를 담고/되돌리고, 복원 단계 수(5→6)

### 5-3. 375px 실측 (`check:mobile`, 계획 29건)

```
375px(계획): 가로 스크롤 없음               375
375px(계획): 한 줄에 지평 3칸                3
375px(계획): 3칸이 세로로 쌓이지 않는다      PASS
375px(계획): 칸 폭이 100px 이상              {"w":110,"h":135}
375px(계획): 접기 버튼이 44px 급             {"h":44}
375px(계획): 제목이 칸 밖으로 새지 않는다    PASS   ← 4절의 그 버그
375px(계획): 접으면 칸이 사라진다 / 개수는 남는다 / 다시 펴진다
1280px(계획): 표 모양 그대로다 (아코디언 아님) · 가로축 머리가 있다
```

스크린샷: `node_modules/.cache/mobile-shots/plan-375.png` · `plan-375-shut.png` · `plan-1280.png`

### 5-4. 확인하지 못한 것

- **실제 Supabase 에서는 돌려보지 못했습니다.** SQL 이 미실행이고, 지시가 "SQL은 출력만"
  이었기 때문입니다. 점검은 전부 가짜 클라이언트(`scripts/fake-supabase.mjs`) 위에서
  돕니다. 실행 뒤 실계정에서 한 번 눌러 보는 일은 남아 있습니다.
- **`check:mobile` 이 4번 중 1번 흔들렸습니다.** 실패 지점은 이번에 건드리지 않은
  **기존 구역**(`mode=reorder` 의 `.file-row` touch-action 측정)에서
  `getComputedStyle(null)` 이었습니다. 다시 돌리면 3/3 통과합니다 —
  요소가 아직 붙기 전에 물어본 경합으로 보입니다. **이 브랜치의 변경과는 무관**하고,
  고치려면 그 구역에 `waitForSelector` 를 넣어야 해서 범위 밖으로 두었습니다.

---

## 6. 사람이 해야 할 일

1. **SQL 실행** — `supabase/setup.sql` 전체를 SQL Editor 에 붙여넣고 Run.
   (조각만 보고 싶으면 `TODO-SQL.md` 의 「🔴 계획 격자 (plan)」 절)
   실행 전에는 계획 탭이 숨고 한 줄로 알려 주며, 나머지 기능은 예전 그대로 돕니다.
2. **검수 후 main 머지** — 이 브랜치는 push 만 했습니다.
3. 영역 이름이 마음에 안 들면 설정 > **계획 영역** 에서 바꾸세요. 시드는
   "하나라도 있으면 건너뛰기" 라, 바꾼 뒤 setup.sql 을 다시 실행해도 되살아나지 않습니다.

---

## 7. 남겨 둔 것 (이번에 하지 않음)

- **격자 안에서 끌어서 칸 옮기기** — 지금은 카드를 열어 지평·영역을 고칩니다.
  끌기는 폰에서 못 쓴다는 판정이 이 저장소에 이미 두 번 났습니다(모달 순서 바꾸기).
  넣는다면 '옮기기' 메뉴 항목 쪽이 맞습니다.
- **관련 항목의 역방향** — A가 B를 가리켜도 B에서 A가 보이지는 않습니다.
  양방향으로 만들면 한쪽을 지울 때 반대쪽을 함께 고쳐야 합니다.
- **지평 자동 승격** — '이번 달' 이 지나면 단기가 저절로 중기로 내려가는 식.
  사람이 안 본 사이에 자료가 바뀌는 동작이라 일부러 두지 않았습니다.
- **주간 리뷰 알림** — 지금은 사람이 버튼을 눌러야 봅니다.
