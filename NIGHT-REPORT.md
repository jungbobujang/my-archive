# 밤샘 작업 보고

- 브랜치: `night-work` (원격에 push 완료)
- `main`: 손대지 않음 — `25e655c` 그대로
- 커밋 10개 / 파일 36개 / +2,008 −318 줄
- 지시받은 11개 작업 **전부 완료**. 건너뛴 작업 없음 (`SKIPPED.md`)
- `.env` 와 `supabase/setup.sql` 변경 0줄 (확인함)
- DB 변경은 실행하지 않고 `TODO-SQL.md` 에 모음
- 모든 작업마다 `npm run build` 통과 확인 후 커밋

---

## 작업별 결과

### 1. PWA — 완료 (`e286e5e`)

- `public/manifest.webmanifest`: standalone, 세로 고정, any 2종 + maskable 2종 아이콘
- `public/sw.js`: 문서는 네트워크 우선(끊기면 캐시된 index.html), `/assets/*` 는 캐시 우선
  (Vite 해시 파일명이라 안전), 폰트 CDN 은 stale-while-revalidate.
  **Supabase 요청은 절대 가로채지 않습니다** — 데이터가 오래된 채로 보이면 안 되니까요.
- `scripts/generate-icons.mjs`: 의존성 없이 Node 내장 zlib 으로 PNG 를 직접 씁니다.
  폰트 없이 "A" 를 사각형 3개로 그려 래스터화합니다. 아이콘 6종 생성.
- 서비스워커는 **운영 빌드에서만** 등록합니다. 개발 중 캐시 때문에 방금 고친 코드가
  안 보이는 사고를 막기 위해서입니다.

### 2. 모바일 UX (380px) — 완료 (`e523073`)

찾아서 고친 문제:

- **상단 버튼 5개가 한 줄에 안 들어가 페이지 전체가 옆으로 밀렸습니다** (가로 스크롤의 주원인).
  좁은 화면에서만 `⋯` 메뉴로 접습니다. DOM 은 하나고 CSS 로만 전환합니다.
- 별표·체크박스·태그·톱니 등이 손가락으로 누르기에 너무 작았습니다.
  보이는 크기는 그대로 두고 `::after` 로 히트 영역만 44px 급으로 넓혔습니다.
- 입력 글꼴이 16px 미만이라 **iOS 에서 입력칸을 누르면 화면이 확대**됐습니다 → 16px.
- 링크를 저장할 때 제목을 못 가져오면 URL 이 그대로 제목이 되는데,
  줄바꿈 설정이 없어 **긴 URL 이 카드를 뚫고 나갔습니다** → `overflow-wrap`.
- 오늘 탭에서 카테고리 배지가 길면 제목을 0폭까지 밀어냈습니다 → 배지 쪽을 먼저 줄입니다.
- 마인드맵의 `+` 버튼이 축소 렌더 때문에 화면상 4~5px 였습니다 → 투명 히트 원 추가 + 확대.
- 검색줄이 칩 2개와 한 줄에 눌려 있던 것, 모달 여백, `100dvh`, 노치/홈 인디케이터 여백.

### 3. 다크 모드 — 완료 (`5224d6c`)

- `src/theme.js` — 저장값은 `system | light | dark`, `<html data-theme>` 에는 언제나
  `light`/`dark` 로 확정해 넣습니다. `system` 일 때만 OS 설정 변화를 구독합니다.
- `index.html` 인라인 부트스트랩으로 **첫 페인트 전에** 테마를 확정합니다
  (없으면 다크 사용자에게 흰 화면이 번쩍입니다).
- 흩어져 있던 하드코딩 색을 전부 토큰화한 뒤 다크 값을 얹었습니다.
  `--ink` 가 다크에서 밝은 색이 되므로, 그 위에 흰 글씨를 쓰던 곳들(`.btn-primary`,
  `.chip-on`, 마인드맵 중앙 텍스트 등)이 안 보이게 되는 문제를 같이 잡았습니다.
- 카테고리 8색은 **hue 는 그대로 두고 명암만 뒤집었습니다.** 같은 카테고리가 라이트/다크에서
  같은 색으로 읽혀야 하니까요. 계산해 보니 8색 모두 대비 8:1 내외로 라이트와 같은 수준입니다.

### 4. 에러 처리 일관화 — 완료 (`1b07d86`)

`components/Toast.jsx` 를 만들고, 조용히 실패하던 곳을 전수 점검해 붙였습니다.

- 특히 **별표·완료 토글이 위험했습니다.** 화면만 바꾸고 DB 실패를 무시해서,
  저장이 안 됐는데도 성공한 것처럼 보이고 새로고침하면 사라졌습니다.
  이제 실패하면 화면을 되돌리고 알립니다.
- Today 의 조회 5개가 오류를 전혀 확인하지 않아, 실패해도 "항목 없음" 으로 보였습니다.
- 백업/복원의 `alert()` 4곳을 토스트로 교체 (모달 위에 뜨도록 z-index 60).
- 모달 안 작업은 기존 인라인 문구를 유지했습니다 — 맥락 안에서 보이는 편이 낫습니다.

### 5. 성능 — 완료 (`bcae98d`)

- 목록 이미지는 이미 `loading="lazy"` 였고 `decoding="async"` 를 더했습니다.
- **`ItemCard` 를 `memo` 로.** 검색어 한 글자마다 카드 24개가 전부 다시 그려지고 있었습니다.
  memo 만 걸면 인라인 화살표 콜백 때문에 무효가 되므로, 부모 콜백을 `useCallback` 으로
  고정하고 소속 없는 항목에 고정 빈 배열을 넘기도록 같이 고쳤습니다.
- `Today` 의 `Badges`, `MindMap` 의 `AddButton` 이 컴포넌트 안에 정의돼 있어
  렌더마다 새 타입이 되고 매번 다시 마운트됐습니다 → 밖으로 뺐습니다.
- 벤더 청크 분리. 앱 코드만 고쳐 배포하면 서비스워커가 캐시한 react/supabase 를
  그대로 재사용합니다 (재다운로드 119 kB → 19 kB).

**번들 (gzip)**

- 앱 코드 19.2 kB
- react 45.3 kB
- supabase-js 57.4 kB ← 가장 큼
- CSS 6.3 kB
- 합계 약 128 kB

`@supabase/supabase-js` 가 절반 가까이 차지하고, 이 앱이 쓰지 않는 realtime 이 들어 있습니다.
더 줄이려면 `postgrest-js` / `auth-js` / `storage-js` 를 직접 쓰는 방법이 있는데,
의존성 구조를 바꾸는 일이라 **하지 않고 보고만 합니다.**

### 6. 코드 정리 — 완료 (`d7b288d`)

- 전체 조회 페이징이 **4곳에 복붙**돼 있던 것을 `fetchAllRows` 하나로.
- `parseTags` 2곳, `ymd` 2곳도 공용으로.
- Esc 로 닫기가 모달 5곳에 흩어져 있었고 **그중 3곳은 의존성 배열이 없어
  매 렌더 리스너를 붙였다 뗐다** 했습니다 → `useEscapeKey` 훅으로 통일.
- 미사용 제거: `extractUrl`, `dialogRef`. 미사용 import 0건 확인.
- 컴포넌트 파일마다 무슨 화면인지 한두 줄 주석.

### 7. README — 완료 (`732d46f`)

현재 기능 전체 기준으로 다시 썼습니다. 폰 설치 방법, 폴더 구조, 커스터마이징 지점,
번들 메모 추가.

> ⚠️ **이 작업 중에 문제를 하나 발견했습니다.**
> `supabase/setup.sql` 이 v1.0 시점 그대로여서 `categories` · `item_categories` ·
> `time_slots` 테이블과 `items` 의 컬럼 6개(`category_id`, `status`, `due_date`,
> `slot_id`, `link_url`, `deleted_at`)가 빠져 있습니다.
> 지금 쓰는 Supabase 에는 손으로 들어가 있으니 앱은 돌지만,
> **새 환경에 setup.sql 만 실행하면 첫 화면부터 깨집니다.**
> 수정 금지 파일이라 손대지 않고, 코드가 실제로 참조하는 컬럼을 역으로 정리한
> 따라잡기 SQL 을 `TODO-SQL.md` 에 담았습니다. 실행하지 않았습니다.

### 8. 일관성 감사 — 완료 (`528cc8a`, 목록은 `DESIGN-CHANGES.md`)

전 화면의 `font-size`/`border-radius`/`padding`/`gap` 을 스크립트로 뽑아 빈도를 세고,
표준에서 벗어난 것만 손봤습니다. **재디자인은 하지 않았습니다.**

- 모서리 반경 11가지 → 토큰 4개. 같은 모달이 화면 폭에 따라 18px/16px 로 달라지던 것 포함
- `btn-ghost`/`btn-danger` 좌우 여백을 `btn-primary` 와 맞춤 (14 → 16)
- 글자 크기 15가지 → 12가지
- **접근성 문제 하나:** `.field input:focus { outline: none }` 이 특이도에서 이겨서
  키보드로 입력칸에 들어가면 **포커스 링이 아예 보이지 않았습니다.** 되살렸습니다.

### 9. 마이크로 디테일 — 완료 (`528cc8a`)

- 색이 바뀌는 컨트롤에만 0.15s 전환, `prefers-reduced-motion` 에서 전부 해제
- 버튼·칩 눌림 상태, 없던 호버 상태 추가
- "선택됨" 과 "호버 중" 이 둘 다 테두리만 바꿔 헷갈리던 것 구분
- **로딩 스켈레톤** 도입 (아카이브 목록·오늘 탭·휴지통). 앱 최초 진입은 그릴 내용을
  아직 모르므로 기존 스피너 유지.
- 빈 상태에 아이콘 + 제목 + 보조 문구 구조.
  검색 결과 없음 화면에 **빠져나갈 '필터 초기화' 버튼이 없던 것**을 보완했습니다.

### 10. 첫인상 — 완료 (`03c14e0`)

- 로그인 카드 구조·톤은 그대로. 소개 한 줄을 현재 앱에 맞게 고치고
  (기존 문구는 링크·할 일이 빠져 있었습니다), 실제로 있는 기능 3가지를 얇은 선 사이에 한 줄로.
- **로그인 화면에 테마 토글 추가** — 다크 사용자가 로그인 전에는 바꿀 방법이 없었습니다.
- 파비콘을 `favicon.svg` 로 정리 (PNG 는 폴백 유지). PNG 생성 스크립트와 **같은 글리프 좌표**를
  쓰고 서로를 가리키는 주석을 달아, 한쪽만 고치는 일을 막았습니다.

### 11. 유료화 대비 구조 — 완료 (`c645edb`), **결제 연동 없음**

- `/pricing` — 라우팅 라이브러리를 새로 넣지 않고 History API 로 처리했습니다.
  주소창에 직접 쳐도 열리는 것을 실제 서버로 확인했습니다.
- 무료/프로 2단 비교표. 기존 카드·배지·버튼 스타일만 사용했습니다.
- ⚠️ 표의 한도(1,000개·100MB 등)는 **자리표시 숫자이고 어디에서도 강제되지 않습니다.**
  `Pricing.jsx` 상단 주석과 화면 하단 문구 양쪽에 적어 뒀습니다. 구독 테이블도 만들지 않았습니다.
- 설정 모달: 화면 테마(3택) / **플랜 '무료' 배지 + 요금제 보기** / 로그인 계정.
  플랜 값은 하드코딩이고, 구독 상태를 읽어 올 자리라는 주석을 남겼습니다.
- ⋯ 메뉴의 테마 순환 버튼은 설정 모달로 합쳤습니다 (같은 설정을 두 군데서 만지지 않게).

---

## 아침에 봐 주셨으면 하는 것

1. **`TODO-SQL.md` 의 따라잡기 SQL** — 가장 중요합니다. 실행 여부는 판단해 주세요.
   지금 DB 에는 이미 들어가 있을 가능성이 높아 `if not exists` 로 감쌌지만,
   컬럼 타입은 눈으로 대조해 주시는 편이 안전합니다.
2. **폰에서 실제 확인** — 브라우저 확장이 연결돼 있지 않아 실기기 검증을 못 했습니다.
   설치 / 오프라인 / 380px 레이아웃 / 다크 모드 세 가지는 눈으로 봐 주세요 (`SKIPPED.md`).
3. **요금제 표의 숫자** — 제가 정한 자리표시입니다. 실제 유료화 계획에 맞게 바꿔 주세요.
4. `night-work` 를 `main` 에 병합할지 — 병합 전 위 1·2번을 먼저 하시길 권합니다.
