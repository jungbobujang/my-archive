# REPORT-SAVE — 저장 실패 가시화 · 보이지 않는 문자 · 초안 디바운스

2026-09-03. 8/31 에 지시됐지만 커밋되지 않은 채 남아 있던 세 가지를 구현했습니다.
(8/31 커밋 `0ee86e3` 의 makeTitle·빈 제목 방어는 이미 origin/main 에 있습니다.)

---

## 1. 저장 실패 가시화

**한 줄로 뭉뚱그리던 문구를 원인별로 나눴습니다.** 예전에는 무엇이 잘못돼도
"저장에 실패했어요. 네트워크와 Supabase 설정을 확인해 주세요." 한 줄이라, 실제로는
로그인이 풀린 것이어도 사람은 와이파이를 쳐다보게 됐습니다.

- `src/supabase.js` 에 `saveErrorMessage(err)` 를 두고 저장 경로 전부가 이것을 씁니다.
  - 세션 만료·RLS 거부 (`401` · `403` · `42501` · `PGRST301`, 메시지의 `JWT`/`row-level security`)
    → **"로그인이 만료됐습니다. 다시 로그인하면 작성 내용은 유지됩니다"**
  - NUL 등 담을 수 없는 문자 (`22P05`) → "저장할 수 없는 보이지 않는 문자가 남아 있어요…"
  - 빈 제목·NOT NULL (`23502`/`23514` + title) → "제목이 비어 저장하지 못했어요…"
  - 그 밖에 자주 나는 것: `23503`(사라진 카테고리) · `23505`(중복) · `22001`(너무 김) ·
    `42P01`/`PGRST205`(표 없음) · `PGRST204`/`42703`(열 없음) · 연결 실패
  - 알아보지 못한 오류는 **원문과 코드를 그대로 붙입니다** (`저장하지 못했어요: … (XX999)`)
  - **포괄 문구는 코드도 메시지도 없을 때의 최후 폴백으로만** 나옵니다
- 알리는 자리는 **모달 안 한 줄(`.form-error`) + 토스트** 둘 다입니다.
  기존 관행(모달 안은 인라인)을 지우지 않고 토스트를 얹었습니다 — 저장에 성공하면 모달이
  닫히고, 실패해도 긴 폼에서는 그 줄이 화면 밖에 있을 수 있기 때문입니다.
- **링크 분리 모드**: 예전에는 `3개 저장 (2개 실패)` 처럼 개수만 말했습니다. 이제
  실패한 **링크 주소와 이유**를 함께 적습니다(앞 2건 + `외 N건`). 한 건도 저장되지 않으면
  모달을 닫지 않고 첫 실패 이유를 그대로 보여 줍니다.
- 같은 문구 체계를 **이미지·파일 올리기 실패, 파일 내려받기 실패, 휴지통으로 보내기 실패,
  빠른 저장(Archive)** 에도 적용했습니다.

## 2. 보이지 않는 문자 정리

- `stripInvisible(text)` → `{ text, removed }`, 여러 칸을 한 번에 터는 `stripInvisibleAll(map)`.
- 저장 직전에 **제목·본문·태그**에서 털어 냅니다.
  - NUL·C0/C1 제어문자(`U+0000~0008`, `U+000B`, `U+000C`, `U+000E~001F`, `U+007F~009F`)
  - 짝 없는 서로게이트 (붙여넣다 잘린 이모지 등)
  - **탭·줄바꿈(`\n`·`\r`)과 정상 이모지(서로게이트 짝)는 그대로 둡니다**
- 하나라도 털었으면 `보이지 않는 문자 N개를 정리했습니다` 토스트를 띄웁니다.
  조용히 고치면 저장된 글이 붙여넣은 것과 다르다는 사실을 나중에 혼자 발견하게 됩니다.
- 입력 중이 아니라 **저장 직전에** 텁니다 — 입력 중에 지우면 커서가 튑니다.
- 자동 제목도 턴 본문으로 짓습니다(화면의 글과 제목이 어긋나지 않게).
- 빠른 저장(한 줄 입력)에도 같은 정리를 겁니다.

## 3. 초안 디바운스 + 상한

- 초안(sessionStorage) 쓰기를 **입력이 멈춘 뒤 500ms** 로 미뤘습니다.
  예전에는 키를 누를 때마다 본문 전체를 JSON 으로 굳혀 넣었습니다.
- **본문에서 URL 을 훑어 링크 목록에 얹는 일도 같은 박자**로 옮겼습니다
  (지시대로 `dirty` 비교·`extractUrls` 를 함께 미뤘습니다).
- **본문이 1MB 를 넘으면 초안을 건너뛰고**, 모달 안에 한 줄로 알립니다.
- 상수는 `DRAFT_DEBOUNCE_MS = 500`, `DRAFT_MAX_BYTES = 1MB` (supabase.js, 점검도 같은 값을 봅니다).

### 이 과정에서 스스로 막은 것 두 가지

1. **멈추기 전에 저장을 누르면 본문의 링크를 잃을 수 있었습니다.**
   → 저장할 때 본문의 URL 을 한 번 더 훑어 담습니다.
2. **✕ 로 뺀 링크가 저장할 때 본문에서 되살아날 수 있었습니다.**
   → '본문에서 한 번이라도 얹어 본 URL'(`offeredUrls`)을 기억해, 뺀 것은 다시 올리지 않습니다.

---

## 판단해서 정한 것 (지시에 없던 자리)

- **토스트냐 인라인이냐** → **둘 다.** 기존 관행(모달 안은 인라인)을 지우는 대신 얹었습니다.
- **`\r`(캐리지 리턴)** → 제어문자지만 **남깁니다.** 줄바꿈 계열이라 지우는 쪽이 더 파괴적입니다.
- **1MB 를 넘겨 초안을 건너뛴 뒤 창을 닫을 때** → **크기와 상관없이 한 번 더 씁니다.**
  건너뛰기는 '타이핑이 밀리는 것'을 막으려는 것이지 글을 버리려는 것이 아닙니다.
  (sessionStorage 가 거부하면 `writeDraft` 가 조용히 삼키고 작성은 계속됩니다.)
- **초안을 건너뛴 상태에서 예전 초안이 남아 있으면** → **지우지 않고 둡니다.**
  새로고침 때 옛 초안이라도 돌아오는 편이 아무것도 없는 것보다 낫습니다.
- **`useOptionalToast`** 를 Toast.jsx 에 더했습니다. 모달이 ToastProvider 없이 홀로 뜨는
  자리(점검 스크립트)에서 알림을 못 띄운다고 저장 자체가 멈추면 안 되기 때문입니다.

## 검증

`npm run build` 통과. 점검 3종 모두 통과 — 신규 케이스 **59가지**를 더했습니다.

- `npm run check` — **56/56** (기존 그대로. 초안이 디바운스로 바뀌어 임시본 확인 두 가지에
  '입력이 멈춘 뒤' 를 기다리는 시간을 넣었습니다)
- `npm run check:files` — **192/192** (133 → 192)
- `npm run check:mobile` — **31/31** (375px·1280px·다크 실제 크롬)

새로 더한 것 중 지시받은 세 가지 회귀:

- **비로그인 저장 토스트** — RLS 거부(42501)를 흉내 내 저장 → 토스트와 모달 안 한 줄에
  "로그인이 만료됐습니다…", 포괄 문구 없음, 모달이 닫히지 않음, 쓰던 제목과 초안이 남아 있음,
  저장 버튼이 다시 눌림
- **NUL 포함 텍스트** — NUL·제어문자·짝 없는 서로게이트가 섞인 본문과 태그를 저장 →
  저장된 행에 제어문자 0개, 이모지는 살아남고, `보이지 않는 문자 4개를 정리했습니다` 안내
- **50만 자 타이핑 지연** — 10만 자에서 20번 이어 치는 동안 초안 쓰기 **0회**(멈추면 1회),
  50만 자(1.5MB)에서는 초안을 건너뛰고 안내 한 줄, 두 경우 모두 입력 처리 3초 이내

점검 스크립트 쪽 보탬:

- `scripts/fake-supabase.mjs` 에 `store.itemsError` 를 더했습니다(객체면 모든 쓰기 실패,
  함수면 `(행) => 오류|null` 로 특정 행만 실패 — 분리 모드에서 한 건만 튕길 때 씁니다).
- 점검은 네트워크로 나가지 않습니다(`globalThis.fetch` 를 막았습니다) — 제목 자동 생성이
  noembed 로 나가면 판정이 연결 상태에 흔들리고 저장이 끝나기 전에 단정하게 됩니다.

## 확인하지 못한 것

- **실제 Supabase 왕복** — 가짜 클라이언트로만 확인했습니다. 진짜 RLS 거부·22P05 의
  `code`/`message` 가 위 표와 다르면 문구가 '알아보지 못한 오류' 쪽으로 떨어집니다
  (그 경우에도 코드와 원문이 그대로 보이므로 포괄 문구로 뭉개지지는 않습니다).
- **실제 브라우저에서 눈으로** — jsdom·헤드리스 크롬까지입니다.
- SQL 변경은 없습니다. 실행할 마이그레이션 없음.
