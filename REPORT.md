# 밤 작업 보고 (2026-08-22)

- 브랜치: `night-work` — **원격에 push 하지 않았습니다.** 로컬 커밋만 4개입니다.
- `main` 은 손대지 않았습니다 (`3199c98` 그대로).
- 시작할 때 `night-work` 가 `main` 보다 10커밋 뒤여서, 로컬에서만
  `main` 위치로 fast-forward 했습니다 (원격 `origin/night-work` 는 그대로).
  그래서 `git push` 하실 때는 `night-work` 가 `main` 을 포함한 채 앞서 있습니다.
- 지시받은 3가지 모두 마쳤습니다. 건너뛴 작업 없습니다.
- 모든 작업마다 `npm run build` 통과를 확인한 뒤 커밋했습니다.
- `.env` · `supabase/setup.sql` 변경 0줄, DB 작업 0건.

## 커밋

- `5d26ed6` fix(모달): 배경 클릭으로 닫히는 나머지 모달 4개도 막기
- `fdc8a7b` feat(새 항목): 링크를 모달 안에서 하나씩 담고 빼기
- `6ea631f` fix(모달): 링크 줄에 물음표 뒤까지 보여 서로 다른 영상 구분
- `b68de46` chore(검증): 모달·좁은 화면 점검 스크립트 2종

---

## 1. 모달 닫힘 방지 — 이전 지시의 남은 절반

이전 밤에 고친 것은 **'새 항목' 모달 하나뿐**이었습니다. 나머지 네 곳
(카테고리 관리 · 시간대 관리 · 설정 · 휴지통)은 그대로 배경만 스쳐도 닫혔습니다.
네 곳 모두 배경 클릭 닫기를 걷어냈습니다.

- 닫는 길은 이제 **✕ · 취소 · Esc** 뿐입니다. 포커스가 빠지거나(blur) 다른 창에
  갔다 와도 닫히지 않습니다 — 창 전환·탭 숨김에 반응하는 코드는 어디에도 없습니다.
- Esc 는 쓰던 게 있으면 **한 번만** 물어봅니다.
  - 새 항목·수정: 열었을 때와 견줘 달라진 게 있을 때만 (수정 모달을 열자마자
    Esc 를 눌렀다고 확인창이 뜨지는 않습니다)
  - 카테고리·시간대: '새 이름' 칸에 쓰던 글자가 있을 때만
- 카테고리·시간대에서 아이콘 고르개가 열려 있으면 Esc 는 **고르개부터** 닫습니다.
  (모달까지 함께 닫히면 고르던 맥락을 잃습니다)
- ✕ · 취소는 눌러야 닿는 자리라 묻지 않고 닫습니다. 대신 새 항목·수정 모달은
  작성 중인 내용을 sessionStorage 에 남겨 두므로, 다시 열면 되살아납니다.
- 덤: 마인드맵의 카테고리 추가 입력칸도 바깥을 눌렀다고 쓰던 이름을 버리지 않습니다
  (빈 칸일 때만 닫힙니다).

임시 보존은 이전 밤 구현 그대로입니다. 키는 항목 id 기준(`ma:draft:<id>`, 새 항목은
`ma:draft:new`)이고, 저장에 성공하면 지웁니다. 이번에 링크 형식이 바뀌었으므로
**예전 형식의 초안(`linkUrl` 한 덩어리)도 읽도록** 했습니다 — 새로고침 한 번에
쓰던 링크를 잃지 않게 하기 위해서입니다.

## 2. '새 항목' 과 '여러 링크' 모달 통합 — 남아 있던 마무리

두 모달을 하나로 합치는 일 자체는 이전에 끝나 있었지만(`BulkAdd.jsx` 삭제됨),
**링크 입력은 여전히 textarea 한 덩어리**였습니다. 링크 하나만 빼려면 긴 주소들
사이에서 그 줄만 골라 지워야 했고, 수정 모달에서는 저장된 링크가 읽기 전용
목록이라 손댈 수조차 없었습니다.

- 링크를 **목록**으로 들고, 한 줄 입력칸에서 Enter 나 '추가' 로 담습니다.
  여러 개를 한 번에 붙여넣으면 공백·쉼표·줄바꿈 아무거나로 나눠 각각 담깁니다.
- 목록의 **✕ 로 한 개씩** 뺍니다. **수정 중인 항목의 링크도 같은 목록에서**
  더하고 뺍니다 (읽기 전용 블록은 없앴습니다).
- 스킴이 빠진 주소(`youtu.be/abc`, `naver.com`)도 알아봅니다 — `parseLinks()`.
- '추가' 를 누르지 않고 저장해도 입력칸에 남은 링크는 함께 담깁니다.
  다른 칸으로 옮기면 조용히 목록으로 들어갑니다. 쳐 둔 것을 잃지 않게 한 장치입니다.
- 같은 링크는 두 번 담기지 않습니다. 내용 칸에 붙여넣은 URL 도 목록으로 올라갑니다.
- 링크가 둘 이상이면 예전처럼 '한 항목에 모두 담기 / 링크마다 개별 항목' 을 고릅니다.
  개수 표시와 저장 버튼도 입력칸에 남은 링크까지 세어 보여 줍니다.

DB 형식은 그대로입니다 — `items.link_url` 한 컬럼에 줄바꿈으로 이어 붙입니다.
**스키마 변경도, 마이그레이션도 필요 없습니다.**

## 3. 검증

Claude 브라우저 확장이 이번에도 연결돼 있지 않아(`Browser extension is not connected`),
**대신 실제 크롬(puppeteer)과 jsdom 으로 직접 돌려 확인**했습니다.
확인한 것들은 스크립트로 남겼습니다 — 저장소에 테스트 러너가 없었기 때문입니다.

`npm run check` (`npm i --no-save jsdom` 후) — **56가지 전부 통과**

- 링크: Enter·'추가' 로 담기, 여러 개 한 번에 담기, 스킴 붙이기, 중복 거르기,
  ✕ 로 정확히 그 줄만 빼기, 포커스가 빠질 때 조용히 담기
- 임시본: sessionStorage 저장, 다시 열면 복원(새로고침과 같은 길), '새로 쓰기' 로 버리기,
  예전 형식(`linkUrl`) 초안도 읽기
- 닫힘: 모달 5개 모두 배경 클릭으로 안 닫힘, 창 전환(blur·visibilitychange) 뒤에도
  그대로, Esc 는 한 번만 묻고 '취소' 면 안 닫힘, 아이콘 고르개가 먼저 닫힘

`npm run check:mobile` (`npm i --no-save puppeteer` 후) — **16가지 전부 통과**

- 375px · 1280px · 다크에서 가로 스크롤 0 (`documentElement.scrollWidth` 실측)
- ✕ 손가락 영역 40x44, '추가' 버튼 54x48, 입력 글꼴 16px (iOS 입력 확대 방지)
- 링크 줄이 옆으로 넘치지 않고, 긴 주소가 ✕ 를 덮지 않음
- 서로 다른 유튜브 링크가 목록에서 다르게 보임
  (여기서 `?v=` 를 떼던 문제를 발견해 `6ea631f` 로 고쳤습니다)
- 스크린샷은 `node_modules/.cache/mobile-shots/` 에 남습니다

judge 류 테스트는 이 저장소에 없습니다 (`*test*` · `*judge*` 파일 0건, package.json 에
test 스크립트 없음). 그래서 위 두 스크립트를 그 자리로 두었습니다.

## 사람이 한 번 봐 주셔야 하는 것

1. **실제 저장 경로** — Supabase 계정으로 로그인한 상태에서 링크 3개짜리 항목을
   한 번 저장/수정해 봐 주세요. 저장 로직 자체는 건드리지 않았지만(형식 동일),
   실제 DB 왕복은 확인하지 못했습니다.
2. **실제 폰** — 375px 는 크롬 뷰포트로 쟀습니다. 손가락 감각은 다를 수 있습니다.
3. **점검 스크립트를 저장소에 둘지** — 앱 의존성은 하나도 늘지 않았지만
   (`dependencies`·`devDependencies` 변화 없음, 도구는 `--no-save` 로만 설치),
   원치 않으시면 `scripts/check-*.mjs` 와 `scripts/mobile-harness/` 를 지우고
   package.json 의 `check` 두 줄만 빼면 됩니다.

## 남긴 흔적

- `node_modules` 에 `jsdom` (또는 `puppeteer`) 이 `--no-save` 로 깔려 있을 수 있습니다.
  `npm install` 한 번이면 package.json 기준으로 정리됩니다.
- puppeteer 가 받은 크롬이 `~/.cache/puppeteer` 에 남아 있습니다(수백 MB).
  다시 쓰지 않으실 거면 폴더째 지우셔도 됩니다.
- 이전 밤 보고서는 `NIGHT-REPORT.md` · `SKIPPED.md` 에 그대로 두었습니다.
