# 모바일 재점검 (375px 기준)

브랜치 `night-work`. 지시받은 항목대로 전 화면을 375px 기준으로 다시 훑고, 찾은 문제를 고쳤습니다.

## 점검 방법 — 먼저 읽어 주세요

**실제 폰·브라우저로 보지 못했습니다.** Claude 브라우저 확장이 연결돼 있지 않고
(`Browser extension is not connected`), 로그인 계정 비밀번호가 없어 로그인 이후 화면을
띄울 수단이 없었습니다. 대신 이렇게 점검했습니다.

- `styles.css` 를 파싱해 **375px 에서 실제로 적용되는 규칙만** 골라내는 스크립트를 만들어
  (미디어쿼리 우선순위·특이도 반영) 터치 대상 크기, 그리드 열 수, 콘텐츠 폭 초과를 계산
- 375px 콘텐츠 폭(343px)에 각 행의 요소 폭을 더해 넘치는지 손계산
- 라이트/다크 토큰 짝 맞춤, 정의 없는 `var()`, 하드코딩 색 잔존 여부 검사
- 매 수정마다 `npm run build` 통과 확인

따라서 **"코드상 이런 크기가 된다" 까지가 이 문서의 보증 범위**입니다.
키보드 동작과 핀치줌은 실제 기기에서 한 번 봐 주셔야 합니다(맨 아래 목록).

---

## 1. 화면별 결과

### 오늘 탭

- **슬롯 그룹** — 문제 없음. 슬롯 제목(12.5px) + 개수 배지, 그룹 간 여백 10px.
  슬롯이 없는 할 일은 "언제든" 그룹으로 모입니다.
- **체크박스 터치 영역 — 고쳤습니다.**
  시각 크기 20px, 히트 영역 36×40px 이었습니다. 모바일에서 보이는 크기를 24px 로 키우고
  히트 영역을 **36×44px** 로 맞췄습니다.
  가로를 44px 까지 넓히지 않은 이유: 옆 제목 버튼과의 간격이 6px 뿐이라
  더 넓히면 제목 왼쪽을 덮어 **제목을 누르려다 완료 처리**가 됩니다.
  세로 44px 를 확보하는 쪽이 실제 오조작을 더 줄입니다.
- 시간대 관리 톱니 — 37×37px → **45×45px**.
- 배지가 긴 카테고리명이면 제목을 밀어내던 문제는 지난 작업에서 처리됨(배지 최대 45%).

### 아카이브 목록 — 카드 그리드

**1열로 바꿨습니다.** 기존에는 375px 에서도 2열이었습니다.

측정한 근거:

- 375px 에서 2열이면 카드 폭 166px → 본문 142px →
  체크(20) + 별표(23) + 간격을 빼면 **제목에 남는 폭이 87px** (한글 6자 남짓)
- 1열로 바꾸면 같은 자리가 **273px** 가 됩니다

`@media (max-width: 400px)` 에서만 1열이고, 썸네일 높이도 120px → 150px 로 함께 키웠습니다.
폭별 결과:

- 320 / 360 / 375 / 390px → **1열**
- 412 / 430px → 2열 (기존 그대로)
- 640px 이상 → 3열, 900px 이상 → 4열 (기존 그대로)

촘촘히 훑고 싶을 때는 리스트 보기(☰)가 그대로 있습니다.
2열로 되돌리려면 `styles.css` 맨 아래 `@media (max-width: 400px)` 블록만 지우면 됩니다.

### 카테고리 카드

**2열 유지됩니다.** (375px 에서 카드 폭 167.5px)

다만 이름이 길면 줄바꿈이 일어나 카드 높이가 들쭉날쭉했습니다.
"유튜브 대본" + 아이콘 + 개수 = 141px 로 본문 폭 139px 를 2px 넘깁니다.
→ 이름에 말줄임(`text-overflow: ellipsis`), 개수에 `flex: none` 을 줘서
**2열을 유지한 채 한 줄로** 끊기게 했습니다.

### 마인드맵

- **가로 스크롤 — 정상.** `.mindmap-wrap` 이 `overflow-x: auto` 이고 SVG 최소 폭이 520px 이라
  375px 화면에서 177px 만큼 옆으로 밀립니다. `overscroll-behavior-x: contain` 이라
  지도를 끝까지 밀어도 페이지가 따라 움직이지 않습니다.
- **핀치줌 — 가능합니다.** viewport 메타에 `user-scalable=no` / `maximum-scale` 이 없고,
  `touch-action` 을 건 곳도 없어 브라우저 기본 확대가 그대로 동작합니다.
- 참고 수치: 375px 에서 지도는 0.66배로 그려집니다. 라벨 16단위 → 화면상 약 10.5px.
  더 크게 보려면 핀치줌을 쓰면 됩니다.
- `+` 버튼은 투명 히트 원(반지름 22단위)이 있어 축소 상태에서도 눌립니다.
  터치 기기에서는 `@media (hover: none)` 으로 항상 보입니다.

### 항목 모달 — 키보드 가림

가장 손볼 게 많았던 부분입니다. 세 가지를 바꿨습니다.

1. **모달을 위쪽에 붙였습니다** (`align-items: start`).
   세로 가운데 정렬이면 키보드가 올라올 때 모달 윗부분이 화면 밖으로 밀려
   제목 입력칸이 보이지 않습니다.
2. **`visualViewport` 로 실제 보이는 높이를 따라갑니다.**
   `position: fixed` 요소는 키보드가 올라와도 레이아웃 뷰포트 기준으로 남아 있어
   `dvh` 만으로는 부족합니다. `hooks.js` 의 `useVisualViewport` 가 `--vvh` 를 갱신하고
   모달이 `max-height: min(90dvh, calc(var(--vvh) - 24px))` 를 씁니다.
3. 포커스 이동 시 브라우저가 끌어올릴 때 입력칸이 화면 끝에 딱 붙지 않도록
   `scroll-margin` 20px.

내용 textarea 가 화면 절반을 먹지 않게 상한(38dvh)도 걸었는데,
**링크 칸(`.link-input`)은 제외**했습니다. 이 칸은 스스로 높이를 늘리고 `overflow: hidden`
이라, 상한을 걸면 링크가 잘려서 보이지 않게 됩니다.

### 카테고리 관리

- 아이콘 버튼 34px → **40px**, 이름 입력칸 34px → **44px**
- 색 점 26px → **30px** (간격 8 → 6). 8개가 한 줄에 들어가야 해서
  44px 는 물리적으로 불가능합니다(8×44 = 352px > 여유 폭 311px). 30px 가 상한입니다.
  히트 영역을 넓히는 방법도 서로 겹쳐서 **엉뚱한 색이 눌리므로** 쓰지 않았습니다.
- 상위 카테고리 `select` 는 아래 "iOS 확대" 항목 참고

### 휴지통

- 복원(50px) + 영구 삭제(76px) + 간격을 빼면 제목에 173px 이 남습니다. 말줄임 처리돼 있어 정상.
- 버튼 높이 34px → **44px**
- "전부 비우기" 는 `window.confirm` 으로 한 번 더 확인합니다(모바일에서도 정상 동작).

### 백업 (내보내기 / 가져오기)

**실제 문제를 하나 찾아 고쳤습니다.**

두 메뉴는 누르는 순간 ⋯ 메뉴가 닫히는데, 진행 상태("복원 중... (1/3)")가
**그 닫힌 메뉴 버튼 안에만** 표시되고 있었습니다. 즉 모바일에서 복원을 걸면
몇 초~몇십 초 동안 **아무 표시도 없었습니다.**

→ 화면 하단에 진행 알약(`.busy-pill`)을 띄웁니다. "백업을 만드는 중..." /
"복원 중 (1/3)". 토스트와 겹치지 않게 한 칸 위(72px)에 둡니다.

---

## 2. 전 화면 공통으로 고친 것

### iOS 포커스 확대 — 지난 작업의 누락 보완

지난번에 `input, textarea, select { font-size: 16px }` 를 넣었는데,
이건 **element 선택자(특이도 0,0,1)** 라 클래스로 크기를 지정한 입력칸들이 이겨서
그대로 14px / 13px 이었습니다. 즉 **다섯 곳은 여전히 확대됐습니다.**

- `.cm-name` (카테고리 이름) 14px
- `.cm-add input` (새 카테고리) 14px
- `.cm-parent select` (상위 카테고리) 13px
- `.due-row input[type="date"]` (날짜) 14px
- `.mm-input` / `.mm-empty-add input` (마인드맵 카테고리 추가) 13px

→ 모두 명시적으로 16px 로 올렸습니다.
(`.search-box input` · `.field input` · `.login-card input` 은 크기 지정이 없어 이미 정상이었습니다.)

### 터치 대상 44px

측정 후 일괄 조정했습니다. **보이는 글꼴·여백은 그대로 두고 `min-height` 만** 줬습니다
(버튼은 내용을 세로 가운데로 맞추므로 모양이 변하지 않습니다).

- 44px 로 맞춤: `.btn-primary` `.btn-ghost` `.btn-danger` `.btn-sm` `.chip` `.tab`
  `.view-toggle button` `.trash-link` `.cat-manage` `.more-toggle`
  `.cm-name` `.cm-add input` `.quick-row input`
- 히트 영역 확장: 별표 39 → **45px**, 태그 30 → **44px**, 톱니 37 → **45px**,
  체크박스 40 → **44px**
- 시간대 순서 ↑↓ 버튼은 폭도 좁아 `min-width: 44px`
- 남는 예외 2개(의도적): 색 점 30px(위 설명), 카테고리 아이콘 버튼 40px

---

## 3. 빠른 저장 입력줄 (한 손 입력)

- 줄 높이 **52px**, 입력칸 자체 터치 높이 **44px**, 글꼴 16px(확대 없음)
- 입력 가능 폭 약 245px, 저장 버튼 44px
- **문구를 고쳤습니다.** 기존 placeholder "빠른 저장: 입력 후 엔터 (!로 시작하면 할 것으로 저장)"
  은 245px 안에 절반도 안 들어가서, `!` 로 할 일을 만드는 기능을 **모바일에서는 알 수가 없었습니다.**
  → placeholder 는 "빠른 저장 — 입력 후 엔터" 로 짧게, `!` 설명은
  **입력 중에만 아래에 한 줄로** 나타나게 했습니다(`:focus-within`).

결론: 한 손 입력에 무리 없는 크기입니다.

---

## 4. 다크 모드 동일 점검

같은 화면을 다크에서 다시 확인했습니다. 레이아웃은 색과 무관하므로 위 수치가 그대로 적용되고,
색 쪽에서 하나를 찾아 고쳤습니다.

- **로딩 스켈레톤이 다크에서 반대로 보였습니다.** 훑고 지나가는 빛에 `--surface` 를 썼는데,
  다크에서는 `--surface`(#22251f)가 바탕인 `--line`(#33372f)보다 **어둡습니다.**
  빛이 아니라 그림자가 지나가는 모양이었습니다.
  → `--skel-sweep` 토큰을 새로 만들어 라이트 `#ffffff` / 다크 `#454a41` 로 분리했습니다.
- 이번에 추가한 진행 알약은 `--ink` 바탕 + `--bg` 글자라 라이트 14.0:1 / 다크 14.2:1 로 동일합니다.
- 검사 결과: 라이트/다크 토큰 짝 맞음, 정의 없는 `var()` 0건, 본문 하드코딩 색 0건.

---

## 5. 실제 기기에서 확인해 주셔야 하는 것

코드로는 확인이 끝났지만, 실기기에서만 판정할 수 있는 항목입니다.

1. **키보드가 올라올 때 입력칸 가림** — 위 세 가지 조치가 iOS Safari / 안드로이드 Chrome 에서
   의도대로 도는지. 특히 항목 모달에서 **내용(textarea)** 을 누른 상태
2. **마인드맵 핀치줌** — 확대 후 지도를 옆으로 미는 동작이 어색하지 않은지
3. **1열로 바뀐 갤러리** — 실제로 더 나은지, 아니면 2열이 나은지 (되돌리는 법은 위에 적어 뒀습니다)
4. **색 점 30px** — 8색 중 원하는 색이 한 번에 눌리는지
